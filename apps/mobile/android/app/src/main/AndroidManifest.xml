<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EMBEDDED_SUBSCRIPTIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- CALL_PHONE/READ_PHONE_STATE/READ_PHONE_NUMBERS/MANAGE_OWN_CALLS come
         from react-native-callkeep's own bundled AndroidManifest.xml via
         Gradle's manifest merger, not re-declared here. FOREGROUND_SERVICE
         is required for Android 11+ to keep call audio running in the
         background, per react-native-callkeep's own documented requirement. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-feature android:name="android.hardware.telephony.euicc" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="damdam" android:host="esim" android:pathPrefix="/activate" />
        </intent-filter>
      </activity>
      <service
        android:name=".DamDamFirebaseMessagingService"
        android:exported="false">
        <intent-filter>
          <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
      </service>
      <service
        android:name=".CallHeadlessTaskService"
        android:exported="false"
        android:foregroundServiceType="phoneCall" />
      <!-- react-native-callkeep's own AndroidManifest.xml (merged in via
           Gradle) declares the CALL_PHONE/READ_PHONE_STATE/
           READ_PHONE_NUMBERS/MANAGE_OWN_CALLS permissions its
           io.wazo.callkeep.VoiceConnectionService class needs, but does
           NOT declare the service itself: confirmed by reading the
           library's bundled manifest directly. Without this declaration,
           Android's Telecom framework has nothing to bind to when routing
           an incoming call, no crash, calls simply never display. This is
           the single riskiest, most silently-wrong-if-mistaken part of
           this change; see the PR description's unverifiable-without-
           a-device list. -->
      <service
        android:name="io.wazo.callkeep.VoiceConnectionService"
        android:label="@string/app_name"
        android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE"
        android:exported="true"
        android:foregroundServiceType="phoneCall">
        <intent-filter>
          <action android:name="android.telecom.ConnectionService" />
        </intent-filter>
      </service>
    </application>
</manifest>
