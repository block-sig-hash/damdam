# Chunk 28 independent review — US-43

Reviewed 24 September 2026 by Codex, including two fresh read-only reviews
with no inherited task context. Verdict: **ACCEPTED after fixes for the
preparatory simulated integration subset** at code SHA
`e00fc368816d73a7e07ce6f02459c66220783d8f`. The complete integrated
journeys, deployed-client compatibility and live release gates are **OPEN**;
this is not acceptance of all US-43 or production readiness.

## Findings, fixes and boundaries

- **High — lost-response test did not prove adoption of the purchased line.**
  The first fresh reviewer found that the fake raised before creating a line,
  then reconciliation returned a fabricated answer. The corrected fake stores
  the line under the original operation before losing the response; a new
  service and database session reconcile that same line. The test asserts the
  persisted attempt and order item resolve, only one provision call occurs,
  installation remains device-reported, and replayed usage in another session
  does not add a second usage record. The second fresh reviewer found no
  concrete software blocker in this path.
- **P2 — populated upgrade was not joined to historical money backfill.** The
  test now upgrades a seeded `0026` database to current head, runs the real
  `LegacyBackfill`, asserts one balanced NGN journal entry tied to the original
  retail transaction and date, no invented service credit, idempotent rerun,
  and unchanged legacy rows. The paid legacy manifest order is preserved but
  is **not** posted by this regression; enterprise historical accounting is
  not proven.
- **P2 — handoff overstated integration and CI evidence.** The handoff and
  status now distinguish the shared-state recovery/installation/usage test
  from separate checkout/refund, enterprise and V02–V05 suites. One complete
  consumer checkout-to-refund, enterprise import-to-offboard/report and
  concurrent app/browser calling-failure journey are still unverified. The
  second fresh reviewer checked before corrected-head CI finished; that run
  later passed, closing the verification-timing finding.

## Verification

| Check | Result |
|---|---|
| Corrected focused recovery and populated-upgrade/backfill regressions | PASS locally on isolated PostgreSQL |
| Both affected PostgreSQL test files | PASS locally with approved access to the test container; 17 existing Alembic path-separator deprecation warnings |
| Ruff on changed API tests; docs links; whitespace | PASS; 488 relative links, none broken |
| [Prior exact-head CI](https://github.com/block-sig-hash/damdam/actions/runs/35924081754) | PASS at original code SHA `18b6f9c`: 1,733 API tests, 5 skipped, 89.92% coverage; mobile/dashboard, Docker, docs, validator, iOS/Android native screenshot jobs green |
| [Corrected exact-head CI](https://github.com/block-sig-hash/damdam/actions/runs/35959855048) | **PASS** at `e00fc36`: API 1,733 passed, 5 skipped, 89.92% coverage (85% gate), Ruff/mypy/OpenAPI, mobile, dashboard, Docker, docs, release validator and both native jobs green; 72 PNGs from 36 flows per platform |

The initial attempt to run both affected files could not reach the local
PostgreSQL container from the filesystem/network sandbox; the approved-access
rerun passed. No production data was used. The full local API suite was not
completed; the exact-head CI PostgreSQL suite is the full-suite evidence.

## Remaining gates

The preserved paid legacy manifest order has no linked transaction or payment
confirmation timestamp in the fixture. Its historical enterprise money/receipt
accounting, a pending issuance job actually resumed after upgrade, a staged
rollback and installed old-client compatibility remain unverified. Separate
suite coverage cannot substitute for the combined consumer, enterprise and
calling failure journeys listed above.

D1–D6 and V01 B1–B5 also remain open. No signed EAS cloud IPA/AAB, TestFlight
or Android internal distribution, consenting physical testers, live eSIM
installation, cellular data or native carrier call evidence exists for this
code. CI simulators/emulators and generic labs do not close those release
gates. PR #135 is a stacked draft on unmerged PR #134; neither PR was merged,
deployed or submitted to stores. Separate authorization is required for those
actions.

The accepted preparatory subset is an eligible base for chunk 29's authorized
pilot preparation. It does not waive any open chunk 28 or release criterion.
