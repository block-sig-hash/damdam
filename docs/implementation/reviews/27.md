# Chunk 27 independent review — US-42

Reviewed 23 September 2026 by Codex, including a fresh independent review of
the final software head. Verdict: **ACCEPTED after fixes for the preparatory
release-gate software and automated native screenshot scope at
`ab5ac44cb4d88176e96eed0595e2ef204458a8e8`.** Signed distribution,
approved disclosures and live release evidence are **EXTERNAL_BLOCKED**; this
is not acceptance of the full chunk, US-42 release criterion or production.

## Reviewed identity and fixes

- Accepted base: chunk 26 merge `247974b5d4daaa9b7caafde5de55a733c4e0776b`.
  Implementation began at `3f95b0968605097e16c6fa6dc460d790f993e88a`;
  the corrected software head is `ab5ac44cb4d88176e96eed0595e2ef204458a8e8`.
  This review/handoff update changes documentation only.
- **P1 — release signoff could pass retired scenarios or untested code.** The
  validator now requires the reset-product and channel-specific PASS matrix,
  current staging ancestry/configuration, freshness, no later tracked changes,
  no hidden FAIL rows and an external release-owner signature. Negative tests
  exercise malformed and replayed signoffs. It still cannot verify the truth of
  external references by itself.
- **P1 — a PR-controlled workflow/status could spoof promotion approval.** The
  trusted default-branch workflow revalidates PR and merge-group heads with a
  status-only dedicated GitHub App token in a protected environment. It fails
  closed until the administrator installs the App, public key and branch rules;
  current `main` protection does not satisfy this design.
- **P1 — fixture/debug artifacts could be confused with distribution builds.**
  Android Release refuses missing signing, a copied debug keystore and harness
  mode; screenshot APKs use a separate build type. iOS Release checks harness
  mode after both Xcode environment files are sourced and selects production
  APNs entitlements. Shell/CI guard tests pass. No approved signed AAB/IPA or
  EAS cloud build has been produced.
- **P2 — native screenshot evidence was initially incomplete or misleading.**
  iOS changed from a Release-style simulator path to a Debug harness build,
  waits for boot, suppresses the harness-only Debug warning overlay and centers
  the picker target before tapping. The first iOS driver timeout produced zero
  images; a later run passed 34/36 flows; the final exact-head run passed all
  flows and screenshot validation. No failed run is counted as evidence.
- **P2 — the Receipts capture clipped amounts and ignored locale formatting.**
  The screen now stacks long descriptions/amounts, formats UTC dates by active
  locale and preserves nonzero sub-cent precision. Regression tests and final
  English/French Android/iOS renders were checked.

The final fresh reviewer found no remaining concrete software blocker at
`ab5ac44` after independently running 21 validator tests, 3 build-guard tests,
41 focused mobile tests, TypeScript and whitespace checks. The final decision
also uses exact-head CI and rendered image inspection below, which were pending
at the moment of that fresh code review.

## Verification

| Check | Result |
|---|---|
| Full mobile Jest after Receipts fix | PASS: 74 suites, 657 tests, 1 existing skip; pre-existing overlapping-`act()` warnings did not fail assertions |
| Final screenshot matrix/PNG suites, mobile lint and types | PASS: 68 focused tests; lint and TypeScript clean |
| Release validator and build guards | PASS: 21 disposable-git validator tests and 3 build-guard tests |
| Documentation links and whitespace | PASS: 487 relative links, none broken; `git diff --check` clean |
| [Exact-head PR CI](https://github.com/block-sig-hash/damdam/actions/runs/35913988787) | PASS at `ab5ac44`: API, mobile, dashboard, Docker, docs, validator and Android emulator screenshot job; iOS is intentionally on-demand/nightly in this PR run |
| [Exact-head on-demand native CI](https://github.com/block-sig-hash/damdam/actions/runs/35914020227) | PASS at `ab5ac44`: iOS simulator and Android emulator Maestro flows and screenshot completeness, with 72 images per platform |
| Rendered critical captures | PASS for fixture UI: inspected English/French Receipts, French Home/Account, and previously failing iOS activation/error screens; no blocking clipping, overflow or Debug overlay |

The Home fixture retains an English plan-name snapshot in French; Receipts
retain purchase-time descriptions. The fixture seller and placeholder
installation art are not approved commercial or legal copy. The screenshots
validate layout and test-state reachability, not purchases, eSIM activation,
cellular attachment, call media or screen-reader/physical accessibility.

## Remaining gates and next eligible work

- D1–D5 still lack approved supplier routes, saleable markets/devices, legal
  seller, merchant/payment approvals and economics. V04/V05 preparatory code
  does not prove live internet calling or native-dialer service.
- D6 lacks the EAS project/account and approved iOS/Android signing identities,
  verified cloud AAB/IPA hashes, Apple team/profile, AASA/APNs configuration,
  release-status GitHub App, protected environment, external public key, strict
  required status/merge queue and release-owner bootstrap. Current branch
  protection is insufficient for promotion.
- TestFlight and Android internal tracks need release-owner authorization,
  signed builds and consenting physical-device testers. No invitations, store
  submissions or distribution occurred.
- Real eSIM provisioning/installation, cellular data and carrier native calls
  remain a hard physical release gate in chunks 29–30. CI simulators, EAS builds
  and generic device labs cannot replace that evidence. Approved disclosures,
  accessibility and browser/enterprise journey evidence also remain open.

Chunk 28 may use this accepted preparatory software as its integrated-testing
base, but must carry these release gates forward. PR #134 remains open and
unmerged; acceptance does not authorize merging, deploying or store submission.
