# Chunk 01 independent review — US-27

Date: 8 September 2026. Reviewer: Codex.
Verdict: **ACCEPTED after review fixes**, for chunk 01's documentation/specification
scope only. Chunk 02 has not been implemented.

## Reviewed commits

- Baseline: `6790c74707a0f3e52cfedb36bc173ec83ca26663`.
- Submitted content: `0f3331a907783edc75663ded8e6cc85208961ec9`.
- Submitted handoff head: `3e99cefcda288747fb8cfe15adaaf9e0ff6d0fc3`.
- **Accepted corrected content: `36e9bf36a2827d108633730ff0899edbd17b97ef`.**
- Branch: `chunk/01-scope-and-specifications` in
  `/tmp/damdam-review-20260907`. The following review-record commit changes only
  review/status/start instructions; start chunk 02 from that branch tip so those
  records accompany the accepted content. Do not start again at `origin/develop`.

The submitted tree was clean. Fixes were committed locally on the supplied branch;
no unrelated changes, merge, push, deployment or live test was performed.

## Findings and fixes

Locations below refer to submitted head `3e99cef`, before correction. Each finding
is resolved within this chunk; implementation of the original app defects remains
in the assigned future chunks.

1. **P2 — Safety checks were retired before supported paths were retired.**
   `docs/testing-qa.md:645` reduced the offline/chaos suite to account isolation;
   the pre-pilot amendment called that the only surviving obligation. A chunk 04
   implementation could therefore change retries/dispatch without required
   transition coverage. Restored applicable full safety tests and strict TDD
   until the feature, old clients and jobs are verifiably retired. Added the
   omitted ledger TDD category to AGENTS and clarified that PostgreSQL tests are
   for backend behavior, while mobile SQLite persistence must be tested as used.

2. **P2 — Screenshot evidence misstated the artifact and existing validation.**
   `docs/implementation/BASELINE.md:96` described 33 PNGs and a missing nonempty
   assertion. The actual September 8 log validates 32 PNGs; the 33rd uploaded
   file is `maestro-report.xml`. Both platform jobs already enforce count 32.
   Corrected the baseline and dependent specs. Chunk 02 now diagnoses differing
   capture outcomes and strengthens the count check to exact screen/locale
   coverage and valid images, with same-count substitution/corruption cases.

3. **P2 — Deployment and release-enforcement claims exceeded the evidence.**
   `docs/implementation/BASELINE.md:47` attributed scheduled deploy skips to the
   API failure and omitted production history. The workflow is push-only; the
   deployment API returns a July 12 production record. Corrected those facts
   without claiming recorded deployment success or current host health. A current
   branch-protection query returned 404; replaced claims of verified enforcement
   with an explicit evidence gap. Also clarified that non-ancestor feature tips
   do not prove changes never landed via squash/rebase.

4. **P2 — Active entry points still enforced the old working split/freeze.**
   `.github/PULL_REQUEST_TEMPLATE.md:21` still required a pilot-freeze exception;
   SETUP directed Codex implementation followed by Claude review. Updated the PR
   template, marked the old bootstrap instructions historical and placed scope
   precedence notices at the top of directly readable specs. Kept the automated
   Claude workflow changes for chunk 02; its output is supplemental, not chunk
   acceptance. No release workflow was changed.

5. **P2 — Module and product-default rules contradicted the assignments.**
   `docs/implementation/MODULE-OWNERSHIP.md:15` prohibited all cross-module work,
   which would prevent schema, quote and safe-retirement integration. Replaced
   exclusive file ownership with scoped delivery responsibility. Made the four
   proposed defaults distinct from already approved family/SOS removal, aligned
   welfare dispositions across active amendments and removed a blanket stop on
   reversible preparation. No founder decision or vendor approval was invented.

6. **P2 — Redaction rules prohibited required authorized service reporting.**
   `docs/security.md:395` banned assigned numbers and line identifiers from all
   exports and fixtures as though they were activation secrets. Separated real
   credentials from personal/service data: authorized line views and scoped
   reports may show needed data; diagnostic/public evidence stays masked and
   tests use synthetic/scrubbed values. Real activation credentials remain barred
   from logs, exports, source and fixtures.

7. **P2 — Account recovery promised unsupported eSIM portability.**
   `docs/prd.md:1370` required recovery of existing service on a new device
   unconditionally. Separated account/service-record recovery from provider-
   supported profile transfer/reinstallation/replacement. Required an honest
   unsupported path and policy-based support/refund handling without reusing a
   one-time code. No additional carrier capability is assumed.

Also normalized trailing blank lines inherited from the supplied packet so
`git diff --check` passes. The original builder handoff remains historical with a
notice pointing readers to the corrected baseline.

## Verification actually performed

All checks below were performed during this review. They do not represent new
application CI or physical-device execution.

| Check | Result |
|---|---|
| `git diff --check 6790c74` after fixes | PASS |
| `bash docs/implementation/tools/check-docs-links.sh` before acceptance metadata | PASS, 229 relative file links; anchors/external URLs are outside this checker |
| `bash -n docs/implementation/tools/check-docs-links.sh` | PASS |
| Isolated temporary fixture using the actual checker: valid relative path, external URL and local anchor | PASS, exit 0 |
| Same fixture with missing local target | PASS, exits 1 and identifies the target |
| Independent Python inspection of story map and chunk dependencies | PASS: 30 assignments, unique US-27–US-43 definitions, 30 mapped rows, backward-only valid dependencies, all five finding sections |
| Byte comparison to base for auth code, CI workflow, release validator and signoff template | Unchanged |
| September 8 GitHub run metadata and iOS job log | Confirmed job conclusions, 32-PNG validation and PNG-plus-XML upload |
| Deployment API, first page with limit 100 | 19 records returned, including July 12 production and July 28 staging |
| Current classic branch-protection query | HTTP 404; current enforcement remains unverified |

The first one-off story checker incorrectly included historical US-01–US-26 in
its expected reset list. It was corrected to inspect §10.5, then passed; no
repository behavior was changed to satisfy that inspection.

Application pytest/Jest/Vitest/lint/type/native suites were not rerun: the diff
changes documentation, instructions and the PR template, with no runtime, schema
or CI workflow change. The new documentation link script was syntax-checked and
exercised in positive/negative fixtures. The current API failure remains for
chunk 02; no claim of green application CI is made.

## Release boundary and acceptance

Keeping runtime signoff changes in chunk 27 is acceptable for this documentation
chunk. Template and validator must change together after feature retirement; the
old matrix cannot certify the reset product. Chunk 27 now explicitly includes
negative cases for FAIL results, future dates and untested runtime/config changes
after the declared tested SHA, plus verification of server-side enforcement.

US-27's chunk 01 criteria are satisfied after the fixes. Chunk 08's design work
under the same story remains outstanding; acceptance here is not whole-story or
production acceptance. D1–D6 remain OPEN and do not block chunk 02.

**Next eligible assignment: chunk 02 / US-42**, using the corrected baseline and
[the start prompt](../handoffs/02-start.md). Preserve the reviewed branch; leave
runtime release-promotion changes in chunk 27 and do not start chunk 03 as part
of chunk 02.
