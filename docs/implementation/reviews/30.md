# Chunk 30 independent review — US-43 release handoff

Reviewed 24 September 2026 by Codex, including fresh read-only sessions with
no inherited task context. Verdict: **ACCEPTED after fixes for the preparatory
signoff validator, template and truthful NO-GO handoff only**, at corrected code
head `c9d110a2747c9a7d12a0dda1a2b0d5d9aa787dcd`. The tested documentation
head was `6e3446d14b62f9896b72cd625a35866286061575`. The complete US-43
release, integrated CI and physical pilot are **NOT ACCEPTED**. This review is
not a release-owner signoff, merge authorization or production-readiness claim.

## Findings and corrections

- **P1 — blank fields and denial text could pass evidence parsing.** The
  original field parser could consume the next field when one was empty, and
  free-form phrases about unavailable builds could be mistaken for evidence.
  Mandatory references now require an exact `sha256:<64 lowercase hex>` digest;
  empty, placeholder and denial text are rejected. Regression cases cover old
  and new fields, channel decisions, physical rows and passing scenarios.
- **P1 — schema and owner identity were insufficiently bound.** A textual
  revision did not prove it matched the tested source commit; disconnected
  migration cycles and generic role labels could pass. The validator now
  checks the tested commit's single connected, acyclic Alembic head and requires
  opaque `owner:<32 lowercase hex>` roster IDs, with separate ownership and
  rollback evidence digests. Regression tests alter the source graph and owner
  fields, then require rejection.
- **P2 — channel and build claims were ambiguous.** Disabled carrier
  configuration must now be exactly `disabled`, EAS and native CI source SHAs
  must match the tested commit, and blocking findings must be `CLEAR`. The
  template explains that these are structural checks: a human release owner
  must inspect artifact content, signatures, accounts and configuration.

The final fresh reviewer reported no remaining concrete code blocker for this
narrow preparatory diff. Its read-only environment could not run the
disposable-git unit tests; the local suite and CI validator job provide the
executed regression evidence. No reviewer treated synthetic hashes or native
simulator screenshots as physical carrier proof.

## Verification at reviewed code head

| Check | Result |
|---|---|
| `python3 -m unittest discover -s scripts -p 'test_validate_release_signoff.py' -q` | PASS, 47 tests |
| `python3 -m unittest discover -s scripts -p 'test_release_build_guards.py' -q` | PASS, 3 tests |
| Python compilation, Ruff on changed scripts, documentation links and `git diff --check` | PASS; 515 relative links before this review record, rerun for final docs head |
| [Exact-head CI](https://github.com/block-sig-hash/damdam/actions/runs/36025079138) at `6e3446d` | **FAIL overall**: release validator, docs, API, mobile and dashboard jobs passed; API 1,733 passed/5 skipped and displayed 90% coverage. Docker/WAL-G job stopped before PITR because Quay denied the pinned MinIO image pull. |
| Android emulator and iOS simulator in that CI run | PASS 36/36 Maestro flows and 72/72 PNGs on each platform. These are fixture UI checks, not live eSIM/data/native-call evidence. |
| `./scripts/test-walg-pitr.sh` with locally cached pinned images | PASS: `PITR_PROOF=PASS`, `PITR_RECONCILIATION=PASS`, 10-second recovery. This does not close the red CI job or production restore gate. |

## Remaining gates and disposition

The [NO-GO dossier](../release/30-READINESS.md) leaves candidate SHA unset.
The founder confirmed there is no approved EAS project, Apple/Google signing
identity, consenting physical tester, live carrier/merchant pilot authority or
spend limit. Consequently no signed EAS IPA/AAB, TestFlight/Play installation,
real eSIM provision, Wi-Fi-off cellular data, native-dialer call, live internet
call or enterprise pilot has been performed. EAS cloud builds, automated native
CI and generic device labs do not replace those hard physical gates. D1–D6,
applicable V01 B1–B5, chunk 28 combined journeys, release-status App and
protected-environment bootstrap, representative restore/rollback, operational
roster and final production signoff remain open.

The CI/operations owner must repair the reproducible MinIO fixture and rerun
the full PITR job; the release owner must later reconcile real evidence to one
frozen source/configuration and sign the immutable record. Draft stacked PR
#137 remains unmerged. No deployment, store submission, invitation, live spend
or production rollback was authorized or performed.
