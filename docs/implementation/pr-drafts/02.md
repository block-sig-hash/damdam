## What this changes

Repairs refresh-token validation when tests use an application clock different
from real time, and extends consistent date validation to organization tokens.
Concurrent refresh attempts now lock the persisted row so only one can issue a
replacement pair. Expiry, signature, audience and stored revocation checks remain.

Replaces screenshot-count assertions with flow-derived exact coverage and decoded
PNG/CRC validation. Parses Maestro YAML and rejects unsupported dynamic/nested
captures explicitly. Adds a bounded iOS driver-startup timeout, docs-link CI and
an explicit supplemental-only boundary for automated Claude review.

## Build chunk and review

- Chunk 02 / US-42; depends on chunk 01.
- Base: `178c448d503eb2c5733c2daa7e12ce65159be769`.
- Corrected content: `4e5ecc9a55b4230c1b73923eff08fbf41b7f64bf`.
- Review: `docs/implementation/reviews/02.md`.
- Continuation: `docs/implementation/handoffs/02-review.md`.
- Independent verdict: ACCEPTED for chunk 02's assigned scope.
- Chunk 01 merged in #100; this PR carries only chunk 02's additional changes.

## Validation

- API: 359 tests pass, 87.65% coverage (unchanged 85% threshold); migrations,
  ruff, mypy and OpenAPI drift pass.
- PostgreSQL review regressions: all 16 pass; nine fail against submitted code,
  including simultaneous reuse of a refresh token.
- Mobile: 74 suites / 415 tests pass; one existing skip. Lint/types pass.
- Screenshot checker: 29 tests pass; actual 32-PNG September 8 iOS artifact
  validates. Four added checker regressions fail on submitted code.
- Dashboard: 17 suites / 66 tests, lint/types and production build pass.
- API Docker build passes for linux/amd64 and linux/arm64.
- Compose configuration, docs links, workflow YAML and whitespace checks pass.

Both [PR CI](https://github.com/block-sig-hash/damdam/actions/runs/34268255191)
and [on-demand CI including iOS](https://github.com/block-sig-hash/damdam/actions/runs/34268285562)
pass at `aeb74f6`. Fresh artifacts independently validate as 32 readable expected
PNGs and 16 successful flows per platform, with zero JUnit errors/failures.
Docker CI includes readiness and WAL-G point-in-time recovery. The acceptance
commit after this tested head changes documentation only; see this PR's checks
for verification of its final head.

The supplemental Claude action skipped analysis under its workflow-change trust
restriction; its green status is not acceptance evidence. Independent Codex
review supplies that evidence. No check, threshold or trust restriction was
weakened.

Staging remains unconfigured: post-#100 deploy failed before server contact
because STAGING_OCI_HOST, STAGING_OCI_DEPLOY_USER, STAGING_OCI_SSH_KEY and
STAGING_API_BASE_URL are unset. This is D6/chunk 26 environment setup, not a
passing deployment or production-readiness claim. The deployment gate is intact.
