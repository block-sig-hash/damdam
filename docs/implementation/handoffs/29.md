# Chunk 29 handoff — physical pilot preparation

Prepared 24 September 2026. **READY_FOR_REVIEW for preparation only; live
pilot EXTERNAL_BLOCKED.** The founder answered that approved EAS project,
Apple/Google signing identities, consenting device testers, live carrier/
merchant access and spending limit are not yet available. No live activity was
performed or inferred.

## Identity

- Branch/worktree: `chunk/29-physical-pilot` at
  `/home/iadamu/dev/damdam-wt/chunk29`; PR pending.
- Accepted preparatory dependency: chunk 28 code review
  `e00fc368816d73a7e07ce6f02459c66220783d8f`, with documentation base
  `33a9fceb9a4beb51ac8c61b88df64f08ba797b4b`; [review](../reviews/28.md).
  Its full consumer/enterprise/calling and external gates remain open.
- Code/review head: pending commit and independent review. This handoff must
  be updated with exact SHA and CI before any acceptance decision.
- No migration, API contract, package dependency or customer data change.

## Result and acceptance evidence

Android production signing now inspects the **effective** release signing
configuration, so EAS Build's injected keystore can pass the same required
approved-certificate fingerprint check as a locally supplied keystore. It
rejects the checked-in debug certificate, missing material and mismatched
fingerprints. CI uses a generated, one-day, test-only JKS to simulate EAS
injection and check positive/negative paths. It is not a signed cloud build.

The [operator runbook](../release/29-PILOT-RUNBOOK.md) separates release-owner
EAS/signing approval, exact-head native CI, approved TestFlight/Play internal
distribution and the hard physical carrier/enterprise gates. The
[blank forms](../release/29-PILOT-EVIDENCE.md) require identifiable source,
device, offer, cost, supplier, ledger, finance and reviewer evidence while
excluding secrets and customer identifiers from the repository.

| Criterion | Result |
|---|---|
| Executable checklist, stop/go and evidence forms | PREPARED; independent review pending |
| iOS and Android EAS cloud signed build with approved identities | BLOCKED: project/signing not approved |
| Exact-head iOS simulator/Android emulator CI | PENDING on this branch; chunk 28 passed at earlier head only |
| TestFlight/Play physical internal installs | BLOCKED: approval/testers/builds absent |
| Real purchase, eSIM, Wi-Fi-off data, native Nigeria call | BLOCKED: D1–D6, devices, merchant/carrier and spend authority absent |
| Enterprise administrator/funding/offboarding pilot | BLOCKED: legal/funding/carrier and consenting organization absent |

## Checks actually run

| Check | Result |
|---|---|
| `bash -n scripts/test-android-eas-signing.sh` | PASS |
| Documentation relative links | PASS; 492 checked, none broken before final handoff |
| `git diff --check` | PASS before handoff |
| Local Android Gradle signing regression | NOT RUN: host has no Java/keytool; CI has JDK 17 |
| `actionlint` via installed local image | Workflow parsed; only three pre-existing ShellCheck style/unused-variable warnings in unrelated steps |
| Exact-head CI | PENDING |

## Compatibility and remaining gates

The Android guard retains local `DAMDAM_RELEASE_*` signing and the separate
debug-signed screenshot harness. For EAS, `DAMDAM_RELEASE_CERT_SHA256` must be
in the approved environment and match the actual injected upload key; there
is no `EAS_BUILD` bypass. The on-demand EAS workflow has not run on this
branch. iOS remote distribution signing is not configured or verified.

No physical device, network, provider, merchant or enterprise pilot was run.
No TestFlight or Play upload/invite, merge, deployment, vendor message or live
spend occurred. Chunk 30 must carry all external gates and any review findings
forward; CI, EAS and generic labs cannot close the real eSIM/data/native-call
release gate. Rollback of this preparatory change is a source revert before
release; no database or irreversible data step is involved.
