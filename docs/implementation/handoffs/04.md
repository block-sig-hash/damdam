# Claude implementation handoff — chunk 04, subchunk 04A

## Identity

- **Chunk and registered story:** Chunk 04 — Retire family, safety and
  app-calling features safely. Story **US-30** (`docs/prd.md` §10.5),
  criterion **AC-30.4** (original finding 1). Parent PR-04.
- **Subchunk delivered:** **04A only** — inventory, and account isolation and
  quarantine for both offline queues. **04B–04E are explicitly not built.**
- **Branch/worktree:** `chunk/04-safe-feature-retirement`, in a separate git
  worktree at `/tmp/damdam-chunk04`. The chunk 02 checkout and the chunk 03
  worktree were not switched, modified or committed to. No PR; nothing pushed.
- **Base SHA:** `12582f101e4ca7e6a6c6d55d93470444b7ec7cd3` — `origin/develop`
  after PR #101, carrying accepted chunk 01 (`dffff91`) and accepted chunk 02
  (`ea3a73b`). Verified with `git merge-base --is-ancestor` for both.
- **Head:** the tip of `chunk/04-safe-feature-retirement`.
- **Working tree:** clean. No stashes.
- **Bundle:** `~/damdam-chunk04.bundle`, outside `/tmp`.
- **Accepted dependency reviews:** `reviews/01.md` and `reviews/02.md`. Chunk 04
  depends on 01 and 02, both accepted and merged. It does **not** depend on
  chunk 03, which remains independently under review.
- **Status:** **READY_FOR_REVIEW for 04A.** The chunk as a whole is incomplete
  by design.

## Result

### The defect, confirmed in code before anything changed

`docs/implementation/retirement/INVENTORY.md` §4 records the full inspection.
In summary, at base `12582f1`:

- `checkin_outbox` and `sos_outbox` were created with
  `client_generated_id, timestamp, latitude, longitude` — **no owner column** —
  in one device-wide file, `damdam-safety.sqlite`.
- `pending()` in both was `SELECT ... ORDER BY timestamp ASC` with **no owner
  predicate**, returning every queued row on the device.
- `performSync()` iterated those rows and called `send(item)`, bound to whatever
  access token was current when the service was constructed.
- `AuthenticatedApp.tsx` memoised both services on **`accessToken` only**. No
  user identity took part in construction, filtering or dispatch.
- `clearSession()` reset the Keychain and nothing else — it did not clear the
  queues and did not stop the 10-second sync timer.
- `PersistedSession` had no user id, so the client could not even name an owner.

**The failure:** A queues offline and signs out; B signs in; the timer fires;
A's rows are POSTed with B's token. The server derives the owner from that
token, so the record is genuinely created as B's. **No server-side check can
detect this** — the mechanism being subverted is the one the server trusts.

### What 04A changes

| Area | Change |
|---|---|
| `services/outboxOwnership.ts` (new) | Shared ownership rules, the owned-table migration and the quarantine, written once for both queues |
| `services/checkInOutbox.ts` | `owner_user_id` column; `enqueue`/`pending`/`remove`/`updateLocation` all owner-scoped; ownership re-checked before the loop, before each send, and after each send resolves |
| `services/sosOutbox.ts` | The same treatment |
| `services/sessionStore.ts` | `PersistedSession.userId` |
| `hooks/useSessionGate.ts`, `navigation/OnboardingNavigator.tsx` | `userId` threaded through `ActiveSession` and `AuthenticatedMobileSession` |
| `navigation/AuthenticatedApp.tsx` | Builds `OutboxOwnership` from the signed-in user and passes it to both services |
| `services/testSqlite.ts` (new) | Test-only adapter running the real SQL against sql.js |
| `docs/frontend-mobile.md` §8.10, `docs/security.md` §10.16 | Amendments recording the rule and its consequence |
| `docs/implementation/retirement/INVENTORY.md` (new) | The required pre-change inventory and the subchunk proposal |

**Ownership is a required constructor argument, not an optional one.** A default
would leave a forgetful caller with a silently dead queue; a compile error is the
better failure. TypeScript located all 13 call sites.

**Legacy rows are quarantined, never adopted.** Rows with no owner move to
`<table>_quarantine` with a timestamp and reason. They are **kept** — they are a
user's data — and never attributed to whoever signs in next. Quarantine runs on
every initialize, so an interrupted migration converges.

**Ownership is a function, re-resolved at dispatch time**, not a value captured
at construction. That is what makes the delayed-callback case safe: a response
arriving after the account switched neither confirms nor consumes the row.

### Deliberate consequence, stated plainly

A session persisted before this change has no `userId`, so it **owns nothing**:
its queues are inert and it cannot enqueue. That is the correct direction to fail
— an event that cannot be attributed to an account must not be sent, and these
features are being retired anyway — but it is a real behavior change for an
existing install, not an oversight. Flagged for review below.

### Deliberately not done — 04B–04E

- **No server route, model, migration or scheduled job was touched.** The
  `app.sos.*` and `app.checkins.*` beat entries still run every 10–30 seconds.
- **No screen, API client or native permission was removed.** Location, contacts,
  microphone and VoIP background mode are all still declared.
- **No dashboard change.**
- **No CLI/WebRTC/CallKit/VoIP-push removal.**
- **No data deletion, no retention execution, no release-gate change.**
- **No welfare/check-in data was deleted.** That disposition is a *proposed
  default* owned by founder/product and is not confirmed. Per
  `SCOPE-DISPOSITION.md`, reversible isolation and migration preparation may
  proceed — which is exactly what 04A is — while irreversible removal waits.

## Acceptance evidence

| Criterion | Test | Result |
|---|---|---|
| Inventory produced **before** behavior changed | `retirement/INVENTORY.md`, written from the base checkout | **PASS** |
| AC-30.4 — A queues offline → logout → B login, no event attributed to B | `sends nothing after A signs out and B signs in` | **PASS** |
| AC-30.4 — B's own sync never picks up A's rows | `B's own sync never picks up A's rows` | **PASS** |
| AC-30.4 — process restart | `survives a process restart with ownership intact` (same SQLite file, new connection) | **PASS** |
| AC-30.4 — delayed callback after the switch | `discards a delayed callback that resolves after the account switched` | **PASS** |
| AC-30.4 — nobody signed in | `sends nothing when nobody is signed in` | **PASS** |
| AC-30.4 — stale work cancelled on logout | `stops its timer so a signed-out session cannot keep polling` | **PASS** |
| Records bound to the original account | `stores the owner alongside every queued check-in` (asserts the real column via `PRAGMA table_info`) | **PASS** |
| Reads scoped by owner | `never returns another account's queued check-ins` / `...SOS` | **PASS** |
| Writes scoped by owner | `cannot remove another account's row` | **PASS** |
| Unowned rows refused | `refuses to queue a row with no owner` | **PASS** |
| Legacy rows quarantined, never assigned | `moves ownerless check-in rows to quarantine instead of assigning them` / `...SOS` | **PASS** |
| Quarantine preserves data | `does not lose the quarantined data` | **PASS** |
| Migration converges | `is idempotent across repeated initialization` | **PASS** |
| Normal operation preserved | `still syncs normally while the same account stays signed in` | **PASS** |
| Existing offline/chaos coverage still passes | `AC-15.4` restart, `AC-16.3` force-quit chaos suites | **PASS** |

**Strict TDD was followed** — safety-transition testing is a strict-TDD category
in `testing-qa.md` §14.13.1. All 16 tests were written first and run against the
unfixed code: **16 failed, 16 total**. They pass after the fix.

**SQLite is tested against a real engine**, as the assignment requires
("test mobile SQLite persistence with its actual storage"). `sql.js` — SQLite
compiled to WebAssembly — runs the same SQL in Jest; an array double would have
passed a broken schema, which is exactly how the original defect survived.

## Checks actually run

| Command and working directory | Result | Output |
|---|---|---|
| `npm test -- --runInBand` (apps/mobile) | **exit 0** | `Test Suites: 75 passed`; `Tests: 1 skipped, 431 passed, 432 total` |
| `npx jest src/services/outboxAccountIsolation.test.ts` **before** the fix | **16 failed, 16 total** | The TDD baseline |
| `npx jest src/services/outboxAccountIsolation.test.ts` after | **exit 0** | 16 passed |
| `npm run lint` (apps/mobile) | **exit 0** | Clean |
| `npm run type-check` (apps/mobile) | **exit 0** | Clean |
| `pytest --cov=app` (apps/api, fresh database) | **exit 0** | `359 passed`, coverage 87.65% — confirms the merged base is green and chunk 02's auth-clock fix is present; no API code changed here |
| `bash docs/implementation/tools/check-docs-links.sh` (root) | **exit 0** | 243 relative links, 0 broken |

Mobile baseline for comparison: chunk 02 accepted at 74 suites / 415 tests. This
branch: 75 suites / 432 tests. No test was removed, no threshold lowered, no
check disabled.

### Checks NOT run, and why

- **Dashboard suites.** **NOT RUN** — `apps/dashboard` is untouched and CI's path
  filter skips that job.
- **Screenshot jobs.** **NOT RUN** — no emulator or macOS runner. No screen
  changed in 04A, so the matrix is unaffected.
- **Docker build check.** **NOT RUN** — no API or dependency change.
- **Any CI run of this branch.** **NOT RUN** — not pushed.

### A note on the API dependency

`sql.js` is added as a **development** dependency of `apps/mobile` only. It is
not imported by any shipped module: `testSqlite.ts` is referenced solely from
`*.test.ts`. This follows the precedent chunk 02 set with `yaml` and `pngjs`,
and it is a pure-WebAssembly package, so it needs no native build on CI's Node 20.

## Migrations and compatibility

- **Server migrations:** none. No schema, no backfill, no `data-model.md`
  amendment needed — nothing server-side changed.
- **Client migration:** the SQLite tables gain `owner_user_id` via
  `ALTER TABLE ... ADD COLUMN` when an old table is found, followed by
  quarantining every ownerless row. It runs on `initialize()`, is idempotent, and
  converges if interrupted.
- **Old-data upgrade:** covered by the quarantine tests, including reopening the
  same database file after a simulated restart.
- **Historical data:** nothing is deleted. Quarantined rows keep their timestamps
  and coordinates.
- **Old-client compatibility:** a client running the previous build is unaffected
  server-side — no contract moved. A device upgrading to this build quarantines
  whatever it had queued.
- **Rollback:** `git branch -D chunk/04-safe-feature-retirement` and
  `git worktree remove /tmp/damdam-chunk04`. On a device, a downgrade would find
  the extra column harmless and the quarantine table unused; the live tables
  would be empty of ownerless rows, which is the intended outcome either way.
- **Secrets:** none added or referenced.

## UI or integration evidence

- **Screenshots / device / locale:** none — no screen or string changed. The
  screen and locale matrix is untouched, so chunk 02's validator still expects
  exactly the same 32 captures per platform.
- **Contract documentation source:** not applicable; no vendor API involved.
- **Fixture versus real storage:** the isolation suite uses a **real SQLite
  engine**, not a fixture. The two pre-existing outbox suites keep their
  in-memory doubles, now owner-aware so they cannot pass against an unscoped
  queue.
- **Physical device evidence:** none. Nothing here needs a handset, but nothing
  here has been observed on one either.
- **Sensitive fields:** the queues hold timestamps and GPS coordinates. No real
  coordinate, account id or token appears in code, tests or fixtures — test data
  is synthetic. Quarantined rows are still personal data and are covered by the
  retention plan owed in 04E.

## Known gaps and handoff

### Open, with impact

1. **The chunk is one fifth delivered.** 04B–04E are unbuilt and listed in
   `retirement/INVENTORY.md` §6. Most importantly, the SOS and check-in **beat
   entries still run every 10–30 seconds** and the server routes are still live.
   04A makes the *client* safe; it does not retire anything.
2. **Native permissions are unchanged.** AC-30.5 is not satisfied. Release builds
   still request location, background location, contacts, microphone and the
   `voip` background mode, all for features being retired.
3. **A pre-US-30 session cannot queue.** See "Deliberate consequence" above.
4. **Quarantined rows are retained personal data** and need a deletion decision
   in 04E under the approved retention policy.

### External gates

None block 04A. The **check-in/welfare disposition remains a proposed default**
owned by founder/product; 04A deliberately does no irreversible welfare removal.
D1–D6 are unaffected.

### Refactoring concerns and tradeoffs for Codex

1. **`ownershipHolds` is checked three times per item** — before the loop, before
   the send, after the send. That is deliberate, and the delayed-callback test
   fails if the third check is removed. It does mean an account switch mid-batch
   leaves the remainder queued rather than erroring; I judged silent preservation
   better than a thrown error in a background timer, but it is a judgement call.
2. **Quarantine is keyed on `client_generated_id` with `INSERT OR IGNORE`.** If a
   later build ever reissued the same client id for a different row, the second
   would be dropped from quarantine. Given these ids are UUIDv4 from
   `react-native-uuid`, I treated that as acceptable.
3. **`useConnection` is a test seam on a production class.** The alternative was
   injecting a connection factory through three call sites for a module being
   deleted in 04C. If you would rather have the factory, it is a small change.
4. **`sql.js` is a new dev dependency.** It is the only way I found to test real
   SQLite on CI's Node 20 without a native build. If you object, the fallback is
   asserting emitted SQL, which is materially weaker and would not have caught
   the missing `WHERE` clause.
5. **`userId` is optional on the session types.** It has to be, for sessions
   persisted before this change. That optionality is what produces the inert-queue
   behaviour; making it required would strand existing users at a type error
   rather than a safe no-op.
6. **The server still trusts the token alone.** A defence-in-depth option is to
   send the client's claimed owner and reject a mismatch server-side. That needs
   an `api-spec.md` contract change, so I left it for 04B where the API is
   already being amended. Worth your view on whether it belongs there.

### Authorization

No merge, deployment, push, store submission, outgoing vendor message, live
purchase or production data deletion. No production data was read or deleted.
Other worktrees untouched.

This handoff is a request for review of **subchunk 04A only**. Chunk 05 has not
been started.
