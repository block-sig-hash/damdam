# Claude implementation handoff — chunk V05

## Identity

- **Chunk and registered story:** V05 — Consumer browser calling. US-48.
- **Branch/worktree path:** `chunk/V05-browser-outbound-calling` at
  `~/dev/damdam-wt/v05`. No PR.
- **Base SHA:** `443c1a3` — V03's accepted head. **Not** branched from V04,
  which is unreviewed; V05's dependencies are V03, 08 and 06/07, all accepted.
- **Head SHA:** `PENDING_HEAD`
- **Working tree:** clean at the head SHA.
- **Accepted dependency review references:** V03 `reviews/V03.md`, V02
  `reviews/V02.md`, chunk 06 `reviews/06.md`, chunk 07 `reviews/07.md`, chunk 08
  `reviews/08.md`. All accepted.
- **Status:** READY_FOR_REVIEW for the browser client scope. The SDK and every
  live-audio claim are **EXTERNAL_BLOCKED**.

## Result

**Previously** the Next.js app served an organization operator (`/home`) and
internal staff (`/admin`), and a consumer had no way to call from a browser.

**Now** `/calls` is a consumer calling area with its own credential, its own
sign-in and no path to either of the other two roles.

### What was built

| Path | What it is |
|---|---|
| `lib/consumerSession.ts` | A third, separately named credential — `sessionStorage`, per tab, cleared with its scoped data |
| `lib/consumerAuth.ts` | Chunk 06's email-identity sign-in; no password, no refresh token stored |
| `lib/callingClient.ts` | V02/V03's contract, reading **only** the consumer token |
| `lib/calling/adapter.ts` | The browser provider boundary, plus `mediaSupport()` |
| `lib/calling/session.ts` | `BrowserCallSession` — the money, reconnect and identity rules |
| `lib/calling/microphone.ts` | A real `getUserMedia` permission request |
| `lib/calling/registry.ts` | The one place an SDK gets installed |
| `app/calls/page.tsx` | Sign-in, keypad, tariff preview, in-call controls, history |

Touched: `messages/en.json` and `messages/fr.json` (new `calling` namespace),
`messages/messages.test.tsx` (a rendered EN/FR check of the new route).

**No API change, no data-model change, no new dependency.** This is a client for
a contract V02 and V03 already established and had reviewed, so there is no
numbered data-model amendment and no `api-spec` edit to make.

### Privilege separation is an absence, not a check

`/calls` reads `damdam_consumer_access_token` and nothing else. The operator
token (`hto_access_token`) and the admin token (`admin_access_token`) are never
read by any code on this path, so an internal user who opens the page is *signed
out*, not elevated — there is no branch that could fall back to them. A role
check can be widened by a later edit; a token this code never reads cannot be.
Three tests assert it by putting each of those tokens in storage and checking
the visitor is still asked to sign in.

Sign-in is chunk 06's email-identity path, which a staff credential cannot
satisfy: somebody with admin access who wants a personal call signs in as
themselves.

### The browser-specific rules

- **A refresh reconciles; it never redials.** The live attempt lives on the
  server and is remembered here by **attempt id alone**. On mount the page asks
  the server what that attempt is doing. Refresh, a restored tab, a second tab
  and a machine waking from sleep are the same question and get the same answer.
  Redialling would be a second call and a second charge for one thing the
  customer did once — the most likely way a browser client bills twice.
- **Unload is best effort and is not the spending control.** `releaseOnUnload`
  tries, and swallows every failure, because a page being torn down may never
  finish a request. What actually bounds the spend is V03's server-side cutoff,
  which does not depend on this code running. The in-call panel says so.
- **`sessionStorage`, not `localStorage`.** A calling credential should not
  survive a closed browser on a shared machine, and a second tab should start
  signed out rather than inherit a call.
- **No refresh token is stored**, so the grant cannot be silently renewed.
- **Sign-out clears the remembered attempt id**, or the next person on that
  browser could reconcile — and be shown the cost of — somebody else's call.

### Credential handling

The only credential-shaped thing that reaches this app is the short-lived
per-device session from `POST /calls/client-session`. There is no Telnyx API
key and no SIP password in the source, the bundle or any storage — the account
key never leaves the server, which is V02's design and unchanged here. Requests
use `credentials: "omit"`: the API is a separate origin authorizing by bearer
token, so no cookie is sent and there is no CSRF surface on this path.

## Acceptance evidence

| Criterion | Test or manual procedure | Result | Evidence path |
|---|---|---|---|
| AC-48.1 personal / no-organization access | `page.test.tsx` "a personal account with no organization reaches the call surface" | PASS | `app/calls/page.test.tsx` |
| AC-48.1 denied admin access | "an internal admin token does not open it" | PASS | same |
| AC-48.1 denied organization access | "an organization operator token does not open it" | PASS | same |
| AC-48.1 scoped data cleared on logout | "signing out clears the scoped data, including the remembered call" | PASS | same |
| AC-48.1 delayed callbacks cannot leak across accounts | `session.test.ts` "a disposed session ignores a late provider callback" | PASS | `lib/calling/session.test.ts` |
| AC-48.2 duplicate tabs / refresh / sleep reconcile rather than redial | `page.test.tsx` "a reloaded tab reconciles the live attempt"; `session.test.ts` two reconcile cases | PASS | both |
| AC-48.2 browser closure is not the control | `session.test.ts` "closing the tab is best effort and is not the spending control" | PASS | `lib/calling/session.test.ts` |
| AC-48.2 no accidental paid retries | `session.test.ts` duplicate-click and same-key retry cases | PASS | same |
| AC-48.2 session expiry | "an expired session is reported as sign-in, not as a call failure" | PASS | same |
| AC-48.3 keypad / manual international entry | "builds a number from the keypad and can correct it" | PASS | `app/calls/page.test.tsx` |
| AC-48.3 tariff and identity preview, committing nothing | "prices a destination without committing anything" | PASS | same |
| AC-48.3 low funds / route disabled without a paid retry | two "keeps the call button unusable" cases | PASS | same |
| AC-48.3 microphone denial, no device, unsupported browser | three `session.test.ts` cases plus `mediaSupport()` | PASS | `lib/calling/session.test.ts` |
| AC-48.3 mute and DTMF follow real capability | two `session.test.ts` cases | PASS | same |
| AC-48.3 backend-derived history, never zero for a missing cost | "never renders a missing cost as zero" | PASS | `app/calls/page.test.tsx` |
| English and French | `messages.test.tsx` renders `/calls` in both; EN/FR key parity enforced | PASS | `messages/messages.test.tsx` |
| Keyboard accessibility | "is reachable by keyboard alone" — every control is a real button or input with no negative tabindex | PASS | `app/calls/page.test.tsx` |
| Unauthorized origins, copied token abuse | — | **NOT RUN — server-side** | see *Known gaps* |
| Live browser microphone, audio, DTMF, identity, actual charge | — | **NOT RUN — EXTERNAL_BLOCKED** | see *Known gaps* |

**All of this is jsdom.** No browser was driven, no Telnyx account was used, no
SDK was installed and no call was placed. Nothing here is evidence that browser
calling works.

## Checks actually run

| Command and working directory | Result / exit code | Relevant output or artifact |
|---|---|---|
| `npx vitest run` (`apps/dashboard`) | **104 passed, 0 failed**, 19 files, exit 0 | 12.4s; 27 of them new |
| `npm run lint` (`apps/dashboard`) | clean, exit 0 | no warnings introduced |
| `npm run type-check` (`apps/dashboard`) | clean, exit 0 | |
| `npm run build` (`apps/dashboard`) | **compiled successfully**, exit 0 | `/calls` present in the route list |
| Rendered browser journey on a real browser | **NOT RUN** | the capture script drives `/gallery` only; see below |
| API / mobile suites | **NOT RUN** | neither app is touched |

## Migrations and compatibility

- **No migration, no schema, no data touched.**
- **No dependency added** — in particular no `@telnyx/webrtc`.
- **No CORS or origin configuration changed.** `security.md`'s V02/V03 amendment
  assigns browser origin policy and secret rotation to chunks 25–26; this chunk
  introduces the browser client, as that text anticipated, and settles neither.
  **The API's allowed origins must include the dashboard's before this page can
  reach it from a deployed host** — named here rather than changed.
- **Old-client compatibility:** `/calls` is a new route; nothing existing
  changes behaviour. The endpoints it calls already existed.
- **Rollback:** delete the route; the `calling` message namespace is inert
  without it.

## UI or integration evidence

- **No screenshots.** `scripts/captureGalleryScreenshots.mjs` drives `/gallery`
  and `screenshot-matrix.json` describes gallery captures only. Capturing
  `/calls` means driving a signed-in consumer journey against a live API, which
  is a harness change and an environment this chunk does not have; I did not
  extend the matrix rather than add entries nothing captures. Chunk 08's
  `gallery-calls` remains the design evidence for these states.
- **Contract source:** V01's `docs/implementation/voice/CAPABILITY-MATRIX.md`,
  which records the browser WebRTC client as DOCUMENTED and the exact supported
  browser/version matrix as **UNKNOWN**. No vendor documentation was fetched for
  this chunk.
- **Sensitive fields:** no token, key, SIP password or activation material in
  any source, fixture, test or document. Test numbers use Ofcom's
  reserved-for-drama `+44 1632 960xxx` range.

## Known gaps and handoff

- **The SDK is not integrated and no live call is claimed.** `resolveCallAdapter()`
  returns an adapter that advertises nothing and refuses to connect. **D1 is
  open** — no Telnyx account — and V01's B1–B5 stand. **Exact missing input: a
  Telnyx account with WebRTC credentials (owner: founder).** Live microphone,
  audio, DTMF, identity and actual-charge proof on supported browsers remains
  pending and is distinct from every mocked test above.
- **The supported-browser matrix is unknown.** `mediaSupport()` detects capture
  support and insecure contexts, which is real and useful, but which browsers
  and versions actually carry a call end to end is exactly what V01 could not
  establish. The page reports what it can detect and claims nothing more.
- **Unauthorized origins and copied-token abuse are server-side properties**, and
  their tests belong with the CORS configuration in chunks 25–26 and with V02's
  own authorization suite, which already covers a token used for an attempt it
  does not own. Nothing in this chunk weakens either; nothing in this chunk
  proves them.
- **The call currency is a constant (`NGN`)**, for the same reason as V04: no
  session endpoint reports one and chunk 09 has a single supported market. A
  multi-currency account needs the server to say which currency the caller
  spends.
- **`deviceId` is the user id.** It should be a stable per-browser identifier so
  that revoking one browser's credential does not describe every browser the
  person uses. Same follow-up as V04, and better done once for both.
- **The browser session controller duplicates the mobile one.** The two apps
  share no build today and creating a workspace package to hold ~200 lines would
  be a larger change than the duplication it removes. Worth revisiting when a
  third client appears — flagged for Codex as a deliberate tradeoff rather than
  an oversight.
- **Cross-client spend tests (V04 + V05 against one balance) belong to chunk 28**,
  as the assignment says, and are not attempted here.
- **No merge, deployment, store submission, vendor message, live purchase or
  production deletion was performed.**

Claude's handoff is a request for review, not acceptance.
