# Chunk 28 implementation handoff — US-43

Prepared 24 September 2026 for independent review. This is **READY_FOR_REVIEW
for the simulated/software integration scope only**, not acceptance of US-43
or a release recommendation. Physical, provider, merchant and signed-build
evidence remains **EXTERNAL_BLOCKED**.

## Identity

- Branch/worktree: `chunk/28-integration-and-recovery` at
  `/home/iadamu/dev/damdam-wt/chunk28`; [draft stacked PR #135](https://github.com/block-sig-hash/damdam/pull/135)
  targets unmerged chunk 27 PR #134.
- Accepted preparatory base: `f65be27bba258a678d80fcd8135495d0fc29db9c`
  (chunk 27 review/docs; software under review there is `ab5ac44`).
- Tested implementation head: `18b6f9cf4e954e7a33d40912ad6ae5574f9270a4`.
  This documentation-only handoff follows that code commit.
- Dependencies: accepted software/preparatory scopes of chunks 19–21 and
  24–27, plus V02–V05 provider-neutral calling scopes. Their external gates
  remain open; no live provider or app/browser SDK capability was inferred.
- Working tree before this handoff: clean. No migration, public API, schema,
  production service or mobile/dashboard application code was changed.

## Result and acceptance matrix

This chunk adds two PostgreSQL integration regressions to the existing matrix:

1. A populated pre-reset `0026_i18n_locales` database with a user, legacy
   organization/paid order, exact NGN transaction reference and amount, eSIM
   profile and pending issuance job upgrades through every later migration to
   `0044_operator_actions`. Original rows are compared before/after and the
   final order, entitlement, line, bulk, offboarding and operator tables are
   present. A separate empty-database migration to head also passed locally.
2. A carrier response is lost after one dispatch; a newly constructed worker
   service and database session reconcile the original operation. The recovered
   line remains neither installed nor network-attached until a device report;
   a replayed cumulative usage poll produces one record and the correct
   allowance. One supplier provision call is asserted. This uses a fake
   carrier at the internal adapter boundary, not Telnyx or a live device.

| Chunk 28 criterion | Evidence at `18b6f9c` | Boundary |
|---|---|---|
| Consumer quote/checkout, supplier recovery, installation/status, usage/top-up/refund | Full API PostgreSQL suite covers checkout, fulfilment, connectivity, usage, controls, refunds and HTTP boundaries; new restart-to-installation-to-usage test joins three services in one database; full mobile Purchase/My Line/Account suites pass | Synthetic products, payment and carrier; no live sale, profile or network |
| Enterprise people/import, fund, partial bulk, offboard and report | Full API suite includes people/import, per-recipient funding, `TestFiftyRecipients.test_the_whole_scenario`, lost/partial bulk results, offboarding and departmental reports; dashboard full suite/build pass | No real enterprise administrator, carrier settlement or invoice |
| Delayed/replayed webhook, accepted-but-lost response, concurrent order/refund, queue crash, stale usage and permission change | Full API suite includes payment conformance, fulfilment `TestLostResponse`/`TestCrashRecovery`, refund ceilings, bulk lost outcomes, usage cursor/staleness and membership/offboarding authorization; new restart test passes | Failure injection is local/fixture; no supplier outage or deployed queue drill |
| Representative migration and historical money/receipt compatibility | New populated-legacy-to-head regression PASS; empty schema to head PASS; existing upgrade/downgrade and receipt snapshot suites pass in exact-head CI | No copy of the actual production database or deployed old-client binary was tested |
| Five original findings | See mapping below; all applicable simulated regressions pass in exact-head CI | Finding 5's physical carrier/device part remains open |

The original findings map to explicit current evidence:

1. Retired offline safety queues are not reachable or rebound to another
   account: chunk 04's safety-transition tests remain in the full API/mobile
   run; mobile `featureRetirement` and account-scoped state tests pass. Actual
   deployed old-client compatibility still needs a device/upgrade check.
2. Consumer purchase and setup are connected in mobile navigation and tested
   by the Purchase, My Line and activation suites; exact-head Android/iOS
   fixture screenshot jobs pass. This is not a live end-to-end purchase.
3. Balance/usage is supplier-derived in the PostgreSQL usage suite; the new
   cross-service test exercises a replayed counter after recovery. Real
   consumption and supplier reconciliation still need chunk 29.
4. Accepted-but-response-lost is held on the original operation and does not
   buy twice; the new restart test and fulfilment/checkout/bulk concurrency
   suites prove the simulated invariant.
5. Controlled API clock and native screenshot CI pass at this head; 72 Android
   and 72 iOS fixture captures were validated in chunk 27 and native CI passed
   again here. Physical eSIM/data/native-call proof is **not** present.

## Checks actually run

| Check | Result |
|---|---|
| Fresh `alembic upgrade head` on isolated PostgreSQL `damdam_chunk28_test` | PASS through `0044_operator_actions` |
| New populated legacy-to-head migration regression on disposable PostgreSQL | PASS; original NGN/history/job rows preserved |
| New recovery/installation/usage regression on separate PostgreSQL `damdam_chunk28_focused` | PASS; one provision and one usage record |
| [Exact-head CI workflow](https://github.com/block-sig-hash/damdam/actions/runs/35924081754) | PASS at `18b6f9c`: API 1,733 passed, 5 skipped, 89.92% coverage (85% gate); API Ruff/mypy/OpenAPI, mobile, dashboard, Docker, docs, release validator and Android/iOS native screenshot jobs all green |
| Mobile `npm test -- --runInBand` | First three-suite parallel attempt had two test timeouts; affected suites passed 39/39 alone. Full serial rerun PASS: 74 suites, 657 passed, 1 skip; pre-existing overlapping React `act()` warnings remain |
| Dashboard `npm test` | First parallel attempt had two five-second timeouts; affected files passed 6/6 alone. Full rerun PASS: 22 files, 126 tests |
| Mobile/dashboard lint and type-check; dashboard production build | PASS; 17 static pages generated |
| API `ruff check .`, `mypy --strict app`, in-memory OpenAPI comparison | PASS; mypy checked 203 source files and OpenAPI matches committed spec |
| Documentation links and diff whitespace | PASS: 488 relative links, none broken; `git diff --check` clean |

The redundant local full API suite was interrupted after about 20% because the
local Docker PostgreSQL volume spent excessive time on fsync; it is **not**
reported as passing. Exact-code-head CI completed the entire PostgreSQL suite
and coverage gate. The focused new tests passed locally on isolated PostgreSQL.

## Compatibility and remaining gates

- No backfill or production migration was applied. The populated legacy
  fixture tests a representative historical shape, not a copy of production.
  Existing downgrade tests cover the preceding migration boundaries; a staged
  deployment rollback and old installed app versions remain unproven.
- No package dependency, API contract, runtime configuration or secret changed.
  The two local test databases contain only fixtures and are not production
  data; their names are recorded here for cleanup after review.
- D1–D6 and V01 B1–B5 remain unresolved. Signed EAS cloud builds, approved
  iOS/Android identities, TestFlight/Android internal distribution, physical
  testers, and real eSIM/cellular-data/native-call evidence remain release
  gates in chunks 27/29/30. CI and generic device labs cannot close them.
- No merge, deployment, store submission, vendor message, live purchase or
  production data operation occurred. PR #135 is a draft stacked review unit;
  merging either it or PR #134 needs separate authorization.
