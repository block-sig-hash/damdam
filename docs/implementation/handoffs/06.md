# Claude implementation handoff — chunk 06 (US-29)

## Identity

- **Chunk and story:** 06 — Implement global identity and account recovery.
  Story **US-29**, PR-03 part A.
- **Branch/worktree:** `chunk/06-identity-and-recovery`, isolated worktree at
  `~/dev/damdam-wt/chunk06`. No PR; nothing pushed.
- **Base SHA:** `d2f5349296e10dcab4e10d3273dc2f7380aa704c` — `origin/develop`
  after PR #106.
- **Content SHA:** `4ca27b0`. Only this handoff follows it.
- **Working tree:** clean. No stashes.
- **Status:** **READY_FOR_REVIEW** — the **complete** chunk, backend and mobile.
  An earlier revision of this handoff deferred the mobile half; it is now
  delivered and this document covers both.

### Dependency gate

Both checked in `STATUS.md` on `origin/develop` **before** any work began:

| Dependency | State |
|---|---|
| Chunk 04 | **ACCEPTED after fixes (04A–04E)** — the complete chunk, PR #104 |
| Chunk 05 | **ACCEPTED after fixes**, integrated on accepted chunk 04 |

## Founder decisions recorded during this chunk

The assignment says to implement email recovery *"if adopted in chunk 01"*. It
was not: `DECISIONS.md` carried it as a **proposed default** and the only
`#### Decision —` entry was the empty template. I stopped and asked rather than
assume, and two decisions were given and are now recorded with dates:

1. **Email is the launch account identity and primary recovery channel.** Phone
   numbers remain *optional* verified identifiers for service-specific uses —
   calling, provisioning, notifications. **Recovery must not require a SIM.**
2. **The pre-verification enumeration signal is removed**, with equivalent
   resistance on the email endpoints, and abuse controls preserved so the
   uniform response cannot be used for SMS or email flooding.

## What changed

### Identity core

`account_identifiers` and `identity_tokens`, a delivery-transport abstraction,
and `IdentityService`. Three rules, all enforced by the database rather than by
control flow:

- **Single-use** via `UPDATE … WHERE consumed_at IS NULL`, so two simultaneous
  clicks cannot both succeed; **expiry exclusive** at the boundary; **purpose
  bound into the row**. A wrong purpose reports `identity_token_invalid`, the
  same as an unknown token — telling a holder they have "the wrong kind" would
  tell them what they have.
- **An unverified claim grants nothing and blocks nobody.** Uniqueness is a
  *partial* index over verified rows, so claiming someone's address can never be
  confirmed once they own it, and equally cannot deny them from verifying it.
- **Only token hashes are stored.**

### Email endpoints

`/v1/auth/email/verify/{request,confirm}` and
`/v1/auth/email/recovery/{request,confirm}`. Recovery issues a session from an
emailed token with no OTP and no phone, revokes every existing session, and
invalidates other outstanding recovery tokens so an older link cannot be
replayed after.

### Enumeration

`POST /auth/otp/request` returned `account_exists` / `account_not_found`
unauthenticated — a working oracle for whether **any** phone number had an
account. Removed, along with both status mappings and their EN/FR strings.
AC-01.7's "direct to login" survives as `is_new_user` on verify, reachable only
by someone who received and submitted the code.

### Mobile: device state belongs to an account

`utils/pinLocalStore.ts` wrote the unlock PIN to a **device-wide** Keychain
service with no account attached, and `clearPinLocally` was called from nowhere
in production — only from its own test. Every session-ending path called
`clearSession()`, which resets the session entry alone.

So A sets a PIN, A's session ends, B signs in on the same handset, and **B's
unlock screen validates against A's PIN** while A's lockout applies to B. The
same shape of defect chunk 04A found in the offline queues.

`LocalPinState` now carries a required `userId`, checked on every read. A record
whose owner does not match reads as absent; a record with **no** owner — written
before this change — is also treated as absent rather than adopted by whoever
signs in next. A pre-US-29 session has no `userId`, so `App.tsx` does not render
the PIN gate for it at all and it falls through to OTP sign-in.

The eSIM compatibility dismissal is keyed per account for the same reason: a
device-wide flag let the first person to dismiss the warning suppress it for
everyone who signed in later, including someone whose device is incompatible.

`clearAccountScopedState()` clears both and runs on **every** session-ending
path. `accountScopedState.test.ts` asserts the boundary, so a future persisted
store that skips account scoping fails a test rather than shipping.

Making `userId` required is what surfaced every call site — the type checker
walked through hooks, screens, navigator and harness. The `activated` onboarding
step's `userId` is required now too: it is built in one place, always from
`result.user.id`, so optional was simply inaccurate.

### Legacy accounts

Migration `0028_account_identity` backfills each existing account's OTP-proved
phone as a verified primary identifier — idempotent, verified by seeding users at
`0027` and upgrading. Nobody is stranded and nobody is asked to re-prove a
number they already log in with. `users.phone_number` is untouched.

## Two corrections worth reading

Both were caught by failing tests, and both were me over-correcting:

1. **I unified the OTP rate-limit budget across signup and recovery**, reasoning
   that separate budgets might leak existence through a side channel. Wrong
   twice: the scope follows the endpoint the caller chose, not account state, so
   it leaks nothing — and sharing a budget broke a real flow, where someone signs
   up, gets PIN-locked and recovers immediately. Reverted; `purpose` is now
   derived separately from the rate scope so a recovery code still cannot be
   redeemed as a login code.
2. **The same mistake in the identity throttle**, where linking an address then
   recovering with it tripped the limit. Also scoped per flow now.

## Acceptance evidence

| Criterion | Test | Result |
|---|---|---|
| Expired links | `test_expiry_is_exclusive_at_the_exact_boundary` — valid one second before, refused exactly at | **PASS** |
| Replayed links | `test_a_verification_token_cannot_be_used_twice`, `test_a_confirmation_token_cannot_be_replayed` | **PASS** |
| Purpose binding | verification↛recovery and recovery↛verification, both directions | **PASS** |
| Concurrent token use | `test_concurrent_use_of_one_token_consumes_it_once` — two threads, barrier, exactly one wins | **PASS** |
| Unverified-email takeover | `test_an_unverified_claim_cannot_take_over_a_verified_identifier` | **PASS** |
| Squatting ≠ denial of service | `test_an_unverified_duplicate_does_not_block_the_real_owner` | **PASS** |
| Constraint, not just service code | `test_the_database_refuses_two_verified_rows_for_one_identifier` | **PASS** |
| Session invalidation on recovery | `test_recovery_revokes_every_existing_session`, `test_recovery_revokes_sessions_held_by_anyone_else` | **PASS** |
| Enumeration-equivalent responses | phone and email, known vs unknown, status + body + nothing sent | **PASS** |
| Throttling survives uniformity | per-identifier, per-flow, applied before the lookup, equal for known and unknown | **PASS** |
| Legacy users retain access | `test_an_existing_phone_user_keeps_access_and_gains_an_identifier`, plus a real migration run | **PASS** |
| Secrets not in storage | `test_the_raw_token_is_never_stored` | **PASS** |

Strict TDD throughout: every test file above was written and run red before its
implementation existed.

## Checks actually run

| Command (apps/api) | Result |
|---|---|
| `pytest` with `TEST_DATABASE_URL`, **fresh database** | **exit 0** — **410 passed** |
| `pytest --cov=app` | **exit 0** — coverage **89.38%** (gate 85) |
| `ruff check .` | **exit 0** |
| `mypy app` | **exit 0** — 98 source files, strict |
| `alembic upgrade head` on an empty database | **exit 0** |
| Migration backfill against seeded legacy users | verified; idempotent on re-run |
| `check_api_spec_drift.py` after regeneration | **exit 0** |
| `check-docs-links.sh` | **exit 0** — 288 links |
| `npm test -- --runInBand` (apps/mobile) | **exit 0** — **56 suites, 352 passed**, 1 skipped |
| `npm run lint`, `npx tsc --noEmit` (apps/mobile) | **exit 0** |

### Not delivered — say so plainly

- **Dashboard contracts unchanged**; that app is untouched, and its suite was
  not run.
- **No mobile UI for the email flows.** The backend endpoints exist and the
  device-state boundary is fixed, but no screen calls `/auth/email/*` yet.
  Linking an address and recovering by email are reachable by API only. That is
  onboarding/account UI and belongs with chunks 18 and 21.
- **Screenshot jobs. NOT RUN** — no emulator. `PinUnlockTarget` and
  `PinSetupTarget` in the harness gained a fixture `userId`, so the captured
  screens are unchanged, but that is reasoned rather than observed.
- **No live email.** No provider configured, nothing sent, no credentials.
- **No CI run of this branch** — not pushed.

## Known limitations and open questions

0. **No mobile UI for email linking or recovery** — see above. The chunk's
   backend contract is complete; the consumer journey is not.
1. **Email is now the account identity, but there is no email *login* flow.**
   Recovery doubles as SIM-free access, which satisfies the decision as stated.
   Whether a first-class email login belongs in 06B or chunk 18 is a scoping
   question for the reviewer.
2. **Token TTL is one hour**, a default I chose. Not derived from a spec.
3. **`NullDeliveryTransport` silently discards.** Deliberate — a half-configured
   provider emailing real people from staging is worse — but it does mean a
   misconfigured deployment fails quietly. Worth a startup warning in 06B.
4. **Pre-existing, not mine:** `test_hto_model_postgres.py` and
   `test_auth_model_postgres.py` fail on a **second** run against the same
   database — they commit fixed values without cleanup. Confirmed by running them
   alone with none of my tests present. Same "fresh database" requirement chunk
   04 recorded. My own tests truncate and are repeatable. I did not modify those
   files, since changing accepted tests would invalidate their review records for
   an unrelated defect.

## Authorization

No push, merge, deployment, live email, vendor message, purchase or production
data change. Chunk 07 has not been started, and this chunk does not mark itself
accepted.
