# Claude implementation handoff — chunk V04

## Identity

- **Chunk and registered story:** V04 — Mobile outbound internet calling. US-47.
- **Branch/worktree path:** `chunk/V04-mobile-outbound-calling` at
  `~/dev/damdam-wt/v04`. No PR.
- **Base SHA:** `443c1a3` — V03's accepted head (`docs: accept V03 after
  independent review`).
- **Head SHA:** `5714aa4`, plus this record's own commit.
- **Working tree:** clean at the head SHA.
- **Accepted dependency review references:** V03 `reviews/V03.md` (**ACCEPTED
  after fixes**; live route EXTERNAL_BLOCKED), V02 `reviews/V02.md` (**ACCEPTED
  after fixes**), chunk 08 `reviews/08.md`, chunk 18 `reviews/18.md`. All four
  dependencies named by the assignment are accepted.
- **Status:** READY_FOR_REVIEW — for the client scope. The SDK integration and
  every live-call claim are **EXTERNAL_BLOCKED**; see *Known gaps*.

## Result

**Previously** the mobile app had four tabs and no way to place a call. V02 and
V03 had built and had accepted the entire backend — authorization, durable call
control, per-device credentials, metering, settlement and enforcement — and
nothing on a handset could reach it.

**Now** there is a Calls tab wired to that accepted contract, a provider
boundary for a calling SDK, and a session controller that holds the money rules.

### What was built

| Path | What it is |
|---|---|
| `src/api/callingClient.ts` | The V02/V03 contract: eligibility, client session, authorize, start, stop, get, list |
| `src/services/calling/adapter.ts` | The vendor boundary — capabilities, events, and `UnavailableCallAdapter` |
| `src/services/calling/callSession.ts` | `CallSessionController`: permission → hold → provider session → dial, and every rule about money and identity |
| `src/services/calling/microphone.ts` | The runtime permission request, and why it cannot succeed yet |
| `src/services/calling/adapterRegistry.ts` | The one place an SDK gets installed |
| `src/services/calling/fakeAdapter.ts` | A controllable adapter for tests and the gallery |
| `src/screens/Calls/CallsScreen.tsx` | Destination, mode, payer, rate preview, history |
| `src/screens/Calls/CallInProgress.tsx` | Phase, elapsed estimate, mute, DTMF, route, hang up |
| `src/screens/Calls/Keypad.tsx` | Twelve keys, for dialling and for DTMF |
| `src/screens/Calls/CallsFlow.tsx` | Owns the controller's lifecycle per signed-in user |

Touched: `src/navigation/ConsumerApp.tsx` (fifth tab), `src/i18n/` (new
`calling` namespace, EN + FR), `screenshotHarness/` (three new targets and
fixtures), `maestro/screens/` (three new flows), `screenshotHarness/reset-matrix.json`
(the `calls` group moves from `pending` to `implemented`).

**No API change and no data-model change.** This chunk adds no endpoint, no
table and no column — it is a client for a contract V02 and V03 already
established and had reviewed. There is therefore no numbered data-model
amendment and no `api-spec` edit, which is the correct outcome rather than an
omission.

### The rules the controller holds

1. **The microphone is asked for before the hold is taken.** A customer who
   declines the prompt after authorization has money reserved against a call
   that never dialled, released only when an expiry sweep runs.
2. **One placement is one hold.** A double tap, a re-render and a retry after a
   lost response all reuse the same idempotency key, so the server converges on
   one attempt.
3. **A client that gives up says so.** Every path that abandons an authorized
   attempt calls `stopCall`, including when the adapter throws on dial and when
   it throws on hangup. V03's own cutoff still runs if the process dies; this is
   the courteous path, not the guarantee.
4. **A disposed session is inert.** Switching account bumps a generation, and
   callbacks still queued inside an SDK land on a generation that no longer
   matches. The next user cannot see or end the previous user's call.

### Carrier versus internet, stated rather than inferred

The screen names the mode and says, in words, that a carrier call is placed by
the phone's own dialler and that DamDam cannot start one. Nothing on this path
touches SIM selection, and changing the payer does not and cannot change which
SIM a carrier call would use. Choosing a work payer discloses that the
organization can see the call happened, its length and its cost — and not what
was said — before the call, not after.

### An internet-only customer needs no eSIM

Calls is a peer of Plans and My Line rather than a section inside it, and
nothing on the path consults an installation, an entitlement or a line. A test
asserts the whole journey — rate preview, call, history — with no eSIM anywhere
in it.

## Acceptance evidence

| Criterion | Test or manual procedure | Result | Evidence path |
|---|---|---|---|
| AC-47.1 production navigation covers grant → rate preview → call → history without installation | `ConsumerApp.test.tsx` "Calls is its own destination"; `CallsFlow.test.tsx` "an internet-only customer previews a rate and calls without any eSIM" | PASS | `src/navigation/`, `src/screens/Calls/` |
| AC-47.1 keypad / manual international entry | `CallsFlow.test.tsx` dials `+441632960011` key by key | PASS | `src/screens/Calls/CallsFlow.test.tsx` |
| AC-47.1 rate / identity / payer preview | `CallsScreen` rate, maximum, available, identity and payer disclosure; covered by the preview tests | PASS | same |
| AC-47.1 mute, DTMF, audio route, hangup follow real capability | `callSession.test.ts` "DTMF is refused rather than silently dropped", "mute is tracked from the adapter" | PASS | `src/services/calling/callSession.test.ts` |
| AC-47.1 elapsed-time estimate | `callSession.test.ts` "the elapsed estimate starts at answer, not at dial" | PASS | same |
| AC-47.1 backend-derived cost and history | `CallsFlow.test.tsx` "history shows a settled cost and never renders a missing one as zero" | PASS | `src/screens/Calls/CallsFlow.test.tsx` |
| AC-47.1 native and internet actions labelled explicitly | `CallsFlow.test.tsx` "names the mode and does not offer to place a carrier call" | PASS | same |
| AC-47.2 account switch cannot show or control the previous user's call | `CallsFlow.test.tsx` account-switch test (real re-render with a new `userId`) and `callSession.test.ts` disposal tests | PASS | both |
| AC-47.2 delayed SDK callbacks cannot leak across accounts | same, plus the generation guard | PASS | same |
| AC-47.2 never leave an untracked billable call | `callSession.test.ts` "a dial that throws still stops the attempt", "hangup reaches the server even when the adapter throws" | PASS | `src/services/calling/callSession.test.ts` |
| AC-47.3 microphone denial | `callSession.test.ts` "microphone denial authorizes nothing" | PASS | same |
| AC-47.3 session expiry, offline, low funds, revoked membership | four `callSession.test.ts` cases using the server's own error codes | PASS | same |
| AC-47.3 duplicate taps | `callSession.test.ts` "a duplicate tap reuses one idempotency key" | PASS | same |
| English/French complete | `i18n.test.ts`, `mobileRendering.test.tsx`, EN/FR key parity verified for the new `calling` namespace | PASS | `src/i18n/` |
| Accessibility | Every control carries a role, a label and a disabled/selected state; keypad keys clear the 48dp target | PASS | `src/screens/Calls/` |
| Derived screenshot matrix | `reset-matrix.test.ts` — `call-setup`, `call-active`, `call-history` captured in both locales on both platforms | PASS | `screenshotHarness/`, `maestro/screens/` |
| Real iOS/Android outbound audio, DTMF, identity, interruptions, cost | — | **NOT RUN — EXTERNAL_BLOCKED** | see *Known gaps* |
| App switch / lock / interruption by a cellular call / network change | — | **NOT RUN — EXTERNAL_BLOCKED** | disclosed in-app; see *Known gaps* |

**Everything above is a Jest run on a developer machine.** No Telnyx account was
used, no SDK was installed, no call was placed and no native build was made.
Nothing here is evidence that internet calling works on a handset.

## Checks actually run

| Command and working directory | Result / exit code | Relevant output or artifact |
|---|---|---|
| `npm test -- --runInBand` (`apps/mobile`) | **620 passed, 1 skipped, 0 failed**, 71 suites, exit 0 | 18.9s |
| `npm run lint` (`apps/mobile`) | clean, exit 0 | no warnings introduced (baseline is zero) |
| `npm run type-check` (`apps/mobile`) | clean, exit 0 | |
| `npx jest screenshotHarness` (`apps/mobile`) | **34 passed**, exit 0 | matrix derived from the Maestro YAML |
| iOS build / Android build | **NOT RUN** | no iOS toolchain on this Linux host; no SDK to build against |
| Maestro capture on an emulator | **NOT RUN** | runs in CI; the flows are added, the images are not |

API, dashboard and backend suites were **not run**: this chunk changes none of
them. The API is untouched.

## Migrations and compatibility

- **No migration.** No schema, no backfill, no data touched.
- **No dependency added.** In particular **no Telnyx SDK** — see below.
- **No native configuration changed.** `RECORD_AUDIO`,
  `MODIFY_AUDIO_SETTINGS` and `NSMicrophoneUsageDescription` are *not* added,
  and chunk 04's retirement guard still passes unmodified.
- **Old-client compatibility:** a client without this build simply has four
  tabs. The endpoints it calls already existed and are unchanged.
- **Rollback:** remove the tab entry; nothing else has side effects.

## UI or integration evidence

- **Screenshots: flows added, images not captured.** The three Maestro flows and
  their harness targets exist and the matrix test verifies them; the emulator
  run that produces the PNGs happens in CI and has not been run here.
- **Contract source:** the SDK facts are V01's `docs/implementation/voice/CAPABILITY-MATRIX.md`,
  which records Telnyx's published React Native peer range (RN `>=0.79 <1.0`,
  React `>=19 <20`) against this repo's RN 0.86.0 / React 19.2.7 and notes that
  the guide requires `--legacy-peer-deps`. **No vendor documentation was fetched
  for this chunk**; V01's record is used as-is and its dates stand.
- **Fixture versus actual provider response:** no provider response exists. Every
  fixture is local.
- **Sensitive fields:** the screenshot fixtures use Ofcom's reserved-for-drama
  `+44 1632 960xxx` range and a reserved Nigerian block, so no capturable image
  contains a number that can ring anybody. No token, credential or activation
  material appears in any fixture, test or document.

## Known gaps and handoff

- **The SDK is not integrated, and no live call is claimed.** `resolveCallAdapter()`
  returns `UnavailableCallAdapter`, which advertises no capabilities and refuses
  to connect. This is the honest state: **D1 is open** — there is no Telnyx
  account — and V01's B1–B5 blockers stand. A stub that resolved would have made
  every screen look finished and would be exactly the "mock passing itself off
  as evidence" the test policy forbids. **Exact missing input: a Telnyx account
  with WebRTC credentials (owner: founder), plus authorized iOS and Android
  devices.**
- **The microphone permission is implemented but cannot succeed.** Android
  `RECORD_AUDIO` is deliberately still absent from the manifest — chunk 08 left
  it out because requesting a permission the product cannot use trains people to
  decline it, and chunk 04's guard asserts its absence. iOS has no React Native
  microphone API at all: `AVAudioSession` raises that prompt, which means the
  SDK raises it. Both arrive together. `requestMicrophone` reports `blocked`
  rather than pretending, and the controller refuses before the prompt when no
  adapter can dial.
- **Chunk 04's retirement guard will need an intentional amendment when the SDK
  lands.** It asserts the absence of `RECORD_AUDIO`, `MODIFY_AUDIO_SETTINGS` and
  `FOREGROUND_SERVICE_PHONE_CALL`. Outbound internet calling is an approved
  scope change under `VOICE-EXPANSION.md`, but *incoming* ringing is still
  retired, so the guard should be split rather than relaxed: keep `voip`
  background mode, PushKit and the contacts/location permissions asserted absent,
  and allow only what outbound audio needs. **I did not touch it in this chunk**,
  because amending a retirement guard without the code that justifies it is how
  a guard quietly stops guarding.
- **Lifecycle behaviour is disclosed, not proven.** `backgroundCall` is `false`
  for every adapter and the in-call panel says audio may stop if the app is
  backgrounded or the screen locked. App switch, screen lock, interruption by a
  cellular call and network change are **untested against any SDK** and remain
  open. The server-side cutoff continuing when the client dies is V03's, and is
  covered by V03's own evidence rather than claimed here.
- **The call currency is a constant (`NGN`).** `/me/session` carries no currency
  and chunk 09 has one supported market, so there is nothing to derive it from
  that would not be a guess. A multi-currency account needs the server to say
  which currency this caller spends; pricing a call against the wrong balance is
  the failure this would cause.
- **`deviceId` is currently the user id.** It should be a stable per-installation
  identifier so that revoking one device's credential does not describe every
  device the person uses. `react-native-device-info` is already a dependency and
  `MyLineFlow` uses it; wiring it here is a small follow-up I did not fold in
  because it changes what a credential revocation means and deserves its own
  review.
- **No merge, deployment, store submission, vendor message, live purchase or
  production deletion was performed.**

Claude's handoff is a request for review, not acceptance.
