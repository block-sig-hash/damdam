# Chunk 30 implementation handoff — preparatory release controls

## Identity

- Chunk/story: 30 / US-43 release candidate and handoff.
- Branch/worktree: `chunk/30-release-handoff` at `/home/iadamu/dev/damdam-wt/chunk30`; [draft PR #137](https://github.com/block-sig-hash/damdam/pull/137), stacked on draft PR #136.
- Base: accepted preparatory chunk 29 documentation head `d7fe6d26c06705afc490ab4e13ebba9dbfa83e37`.
- Initial software submission: `428d6a84fa4a9e5e5a20409c07a353204678d0dc`. Fresh reviews found an empty-field parser defect (corrected `91b5769`), qualified missing-evidence prose (corrected `aaeb34a`), and the same omission in older mandatory approval/scenario fields (corrected code head `66f97abac46e87e524b468191ce688b92eaa42ab`).
- Worktree: this handoff/status identity update is documentation-only and pending commit; verify final cleanliness before review. No unrelated changes intentionally included.
- Accepted dependencies: [25](../reviews/25.md), [27](../reviews/27.md), [28](../reviews/28.md), [29](../reviews/29.md). All are preparatory where external evidence remains open.
- Status: **READY_FOR_REVIEW for preparatory validator and NO-GO dossier only**; full release scope **EXTERNAL_BLOCKED**.

## Result and scope

The prior release-signoff validator already required a release-owner signature,
exact tested code/config ancestry, channel/scenario matrix and physical device
rows, but did not demand exact-head EAS or native CI references, physical
internal installations, disposition of blocking findings or founder-approved
pilot limits. It now rejects omitted/placeholder/blocked mandatory references,
EAS/native source SHAs that differ from the tested commit and any review status
other than `CLEAR`. The same negative-evidence rule now applies to existing
merchant/schema/build approvals, channel decisions, physical device rows and
required scenario proof. The template tells the release owner what to verify
and explicitly limits what the validator can prove.

The [NO-GO dossier](../release/30-READINESS.md) records a provisional version
and schema inventory, open technical/external findings, role assignments still
needed, stop/rollback criteria and a gated rollout/store-asset checklist. It
does **not** create a release candidate, apply infrastructure or assert a live
pilot. The old chunk 29 EAS/Gradle signing compatibility risk remains open.

## Acceptance evidence

| Criterion | Result | Evidence |
|---|---|---|
| Reject missing EAS/cloud, native CI, internal installs, compatibility, limits and blocking finding evidence | PASS in disposable-git unit tests, including empty-field and qualified-absence regressions | `scripts/test_validate_release_signoff.py` (36 tests) |
| Reject explicitly unapproved merchant/schema, channel decision, physical-device and required scenario proof | PASS in disposable-git unit tests | Same test file |
| Reject EAS/native CI evidence from a different source SHA | PASS in disposable-git unit tests | Same test file |
| Reconcile real build, physical, carrier and enterprise evidence | **NOT RUN / EXTERNAL_BLOCKED** | No approved accounts, signing identities, testers, live provider access or spend limit |
| Resolve open combined journeys, production operation and final candidate | **OPEN** | [NO-GO dossier](../release/30-READINESS.md) and dependency reviews |

The passing tests use synthetic references and disposable git histories; they
do not verify remote build IDs, signatures beyond the fixture, real devices,
payments or providers.

## Checks actually run

| Command at repository root | Result |
|---|---|
| `python3 -m unittest discover -s scripts -p 'test_validate_release_signoff.py' -q` | PASS, 36 tests |
| `python3 -m py_compile scripts/validate_release_signoff.py scripts/test_validate_release_signoff.py` | PASS |
| `/home/iadamu/damdam-api-venv/bin/ruff check scripts/validate_release_signoff.py scripts/test_validate_release_signoff.py` | PASS |
| `bash docs/implementation/tools/check-docs-links.sh` | PASS, 515 relative links after handoff |
| `git diff --check` | PASS at corrected head; rerun after identity update |
| Full API/mobile/dashboard/native CI | **PENDING** for corrected code head. Earlier chunk 30 CI runs were superseded by review fixes; Docker/WAL-G failed twice because Quay returned `unauthorized` for pinned MinIO test images. Not counted as PASS |

The system `ruff` command was absent; the existing API virtual environment's
Ruff binary ran successfully. Seven new checks were observed failing before
the validator implementation. Fresh reviewers reproduced an empty-field parser
fail-open, qualified negative text in new references, and qualified negative
text in older merchant/schema/scenario fields. All three are corrected,
bringing the suite to 36 passing tests. The reviewers could not run the disposable-git tests in their
read-only environments because no writable temporary directory was available;
these local results and later CI must be assessed separately.

## Migrations, compatibility and integration

- No migration, mobile binary change, API contract change, dependency or secret
  was added. Source Alembic head observed as `0044_operator_actions`; deployed
  schema and runtime configuration are not verified.
- The signoff template is intentionally stricter. An older signoff omitting
  newly required fields will be rejected. This is a production-promotion
  safety change, not a migration of historical signoffs or tags.
- Actual EAS signing on Gradle 9.3.1, app-store credentials, installed old
  clients, provider adapters and representative staging rollback remain
  unverified. No production infrastructure or data was touched.
- Rollback of this change is a code revert, but weakening the release gate
  should require a separate security review and cannot be used to fabricate
  missing evidence. Operational rollback for a future launch remains in the
  NO-GO dossier and must be rehearsed on representative data.

## Known gaps and review request

- The validator checks exact source SHAs and signed textual references. It
  cannot verify external artifact content or configuration compatibility;
  that is the release owner's signed responsibility. The trusted release-status
  App, protected environment, public key and merge queue still need admin
  bootstrap. A local passing test is not a promotion authorization.
- No EAS build, TestFlight/Play installation, physical eSIM/data/native-call
  test, internet-call media proof, enterprise pilot, live merchant/carrier
  reconciliation, founder pilot limit or named operations roster exists.
  D1–D6 and applicable V01 B1–B5 remain open.
- Full CI is additionally blocked by the pinned MinIO server/client Quay tags:
  two chunk 30 runs saw `unauthorized`/no manifest during the WAL-G drill.
  The [official project](https://github.com/minio/minio) now recommends source builds for current MinIO images;
  no unverified third-party image was substituted. Restore a trusted,
  reproducible S3-compatible test image and rerun the full drill before any
  software-complete readiness claim.
- Independent Codex: review the exact diff, fail-closed cases and truthfulness
  of the NO-GO dossier. Record accepted preparatory scope only if justified.
  Do not mark the full release, store submission or production rollout ready.
- No merge, deployment, store submission, invitation, live purchase or spend
  performed. PR #137 remains draft and stacked on unmerged PR #136.
