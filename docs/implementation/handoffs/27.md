# Chunk 27 implementation handoff — US-42

Prepared 23 September 2026 by Codex under the user's explicit authorization
to implement and review in a fresh session. This is a request for review,
**not acceptance or production signoff**. The first fresh review returned
**CHANGES_REQUIRED**; a second independent review must assess the corrected
head after CI.

## Identity

- Branch/worktree: `chunk/27-release-gates` at
  `/home/iadamu/dev/damdam-wt/chunk27`.
- Accepted integration base: `247974b5d4daaa9b7caafde5de55a733c4e0776b`
  (`develop` after PR #133).
- Initial implementation commit: `3f95b0968605097e16c6fa6dc460d790f993e88a`.
- Corrected review head: pending follow-up commit and CI.
- Accepted dependency records read: [02](../reviews/02.md),
  [04](../reviews/04.md), [08](../reviews/08.md),
  [21](../reviews/21.md), [24](../reviews/24.md),
  [26](../reviews/26.md). V04/V05 accepted provider-neutral scope and the
  [calling amendment](../VOICE-EXPANSION.md) were also applied.
- Status requested: **READY_FOR_REVIEW for the preparatory software scope**;
  distribution-signed builds, store disclosures and live release evidence
  remain **EXTERNAL_BLOCKED**.

## Result

The prior promotion validator intentionally failed every signoff after chunk
04 retired obsolete SOS/incoming-call scenarios. It is now a Python
standard-library validator behind the existing shell entry point. It reads the
committed artifact rather than a dirty working tree, binds its filename and
internal SHA to a tested ancestor, refuses any other tracked change after that
commit, checks seven-day UTC freshness including future dates, and requires
exact common plus channel-specific PASS scenarios, physical Android/iOS rows,
channel eligibility/denial evidence, merchant and applicable carrier
configuration, signed artifacts, privacy/payment review, accepted dependency
ancestry, mock-mode disabled and incident/rollback ownership. The template
documents the same matrix. CI runs the validator's negative tests. This is a
structural gate: it cannot prove the truth of external evidence references or
detect untracked remote configuration drift by itself.

After first review, the gate additionally requires a release-owner Ed25519
signature from outside the PR, checks the exact current staging head, rejects
duplicate/indented FAIL tables and runs trusted base-branch code instead of
PR-head code. A matching custom PR-head status is emitted. An administrator
must first install that workflow on `main`, configure its public key, require
`release-signoff/trusted` with strict up-to-date checks and restrict rule/key
administration. The second fresh review found that a PR-head status alone can
go stale after staging advances or the date expires. A required merge queue
now revalidates the merge-group SHA from trusted default-branch workflow code,
and the release owner must freeze staging during that queue window. None of
that bootstrap is done; current protection is
insufficient (`strict=false`, no required PR review). A dishonest authorized
signer could still attest to false evidence; CI cannot prove provider truth.

Android production `release` no longer uses the checked-in debug key; missing
distribution signing, its debug certificate even from a copied keystore, or
screenshot-harness mode fail closed. It requires an externally approved
certificate fingerprint.
The fixture screenshot build has a separate `screenshot` type. iOS Release now
selects production APNs entitlements; no Apple distribution team/profile is
configured. Retired iOS Hajj/location purpose strings were removed, without
inventing microphone permission before a real outbound SDK is integrated.

The mobile English/French screenshot manifest now includes real Home, Account
and Receipts components and Maestro flows on both platforms. Enterprise and
internal browser journey captures remain explicitly pending. The
[release inventory](../release/27-SIGNING-PRIVACY.md) records actual native
permissions, optional SDK data, hosted checkout, signing commands, store and
terms work, accessibility limits and D1–D6 inputs. Numbered amendments update
the affected specs. No schema, API contract or production service changed.

## Acceptance and checks

| Check | Result at implementation commit |
|---|---|
| Validator positive and negative tests using disposable git histories | Initial 15 PASS; corrected gate 21 PASS locally, including signature tampering, missing key, staging drift, hidden FAIL rows and merge-group SHA |
| Current branch as promotion input | Correctly REFUSED: no release signoff artifact |
| Full mobile Jest | PASS: 73 suites, 653 tests; 1 existing skip |
| Focused screenshot matrix and PNG suites | PASS: 66 tests |
| Mobile ESLint and TypeScript | PASS |
| Documentation relative links | PASS: 472 links |
| Python syntax, shell syntax, release entitlement plist parse, git diff whitespace | PASS |
| Android debug build, screenshot APK/emulator captures and unsigned-release refusal | NOT RUN locally: no Java runtime; PR CI owns these checks |
| iOS simulator images, distribution archive and physical installation | NOT RUN locally: Linux host and no Apple signing access; D6 open |
| Actual provider/merchant/real-call/store disclosures and physical-device proof | NOT RUN: D1–D6 and V01 blockers remain open |

The Jest run emitted pre-existing overlapping-`act()` warnings in purchase and
My Line suites; assertions passed. The three new captures are declared and
tested against the exact Maestro matrix, but rendered CI artifacts must still
be inspected after the PR run. No claim is made that a fixture call produced
media or that a simulator is a physical-device test.

## Compatibility and configuration

- No migration, backfill, API/schema change or deletion of historical
  release tags/signoffs.
- Android distribution signing expects `DAMDAM_RELEASE_KEYSTORE_PATH`,
  `DAMDAM_RELEASE_STORE_PASSWORD`, `DAMDAM_RELEASE_KEY_ALIAS` and
  `DAMDAM_RELEASE_KEY_PASSWORD`, plus approved
  `DAMDAM_RELEASE_CERT_SHA256` from an approved secret manager. No values are
  stored in this handoff. The screenshot type requires
  `SCREENSHOT_HARNESS_MODE=true`; production release rejects it.
- iOS Release requires Apple distribution access and validated APNs/AASA
  configuration. Rollback of this branch before release is a source revert;
  after any real release, use the incident/runbook and compatible database
  rollback procedures rather than overwriting historical tags.
- No new package dependencies, external API calls, vendor messages, purchases,
  merge, deployment, store submission or production data operation occurred.

## Review focus and remaining gates

- Independently probe the validator against malformed Markdown, git ancestry,
  channel-dependent omissions, fake/missing references and changes outside the
  signoff after the tested SHA. Ensure trusted workflow provenance, sidecar
  verification, status binding and required branch protection are sound.
- Inspect the Android Gradle build-type split in a clean CI runner, including
  the expected refusal of `bundleRelease --dry-run` without signing and the
  complete new Maestro screenshots; visually inspect English/French images.
- Reconcile iOS production entitlements with the actual signed archive once
  D6 access exists. Review merged permissions and third-party manifests before
  filing store disclosures.
- D1–D6 remain open. No Telnyx route/SDK, merchant approval, saleable market,
  legal seller, signed distribution build, physical device matrix or approved
  support owner can be asserted from this implementation. The whole chunk and
  US-42 release criterion are not complete on software checks alone.
