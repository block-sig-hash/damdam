# Claude implementation handoff — chunk 23

## Identity

- **Chunk and registered story:** chunk 23 — Build bulk orders, assignment and
  activation requests. US-40 (PR-14 part A).
- **Branch/worktree path:** `chunk/23-enterprise-bulk-provisioning` in
  `~/dev/damdam-wt/chunk23`. No PR.
- **Base SHA:** `3c40d59` — the tip of `chunk/22-enterprise-people-and-imports`.
- **Head SHA:** `3a206c6`, plus this record's own commit.
- **Working tree:** clean at the head SHA.
- **Accepted dependency review references:** chunks [11](../reviews/11.md),
  [14](../reviews/14.md), [15](../reviews/15.md) and [17](../reviews/17.md) are
  each ACCEPTED. **Chunk 22 is not** — it is READY_FOR_REVIEW and this branch is
  cut from it, at the founder's explicit direction to continue the enterprise
  chain rather than wait. If chunk 22 changes under review, this branch rebases
  onto it and the recipient tests are the first thing to re-run.
- **Status:** READY_FOR_REVIEW. Carrier provisioning is **EXTERNAL_BLOCKED** and
  deliberately not wired; see *Known gaps*.

## Result

### Previously

An organization could describe its people (chunk 22) and nothing else. Buying
for them meant fifty consumer checkouts, one per person, with no shared
progress, no per-line funding, no way to resume a half-finished run and no way
to hand a line to somebody who has no account with us.

### Now

An administrator selects people, sees which of them cannot be served and why,
funds one hold per line, provisions them independently, resumes what failed
without ever retrying what was merely *lost*, cancels individual lines, and
hands each recipient a single-use invitation to claim their own.

**API — new `app/bulk` package (9 endpoints).** Eight under
`/v1/organizations/{id}` for the buyer; one under `/v1/me` for the recipient.

**Schema — migration `0039_bulk_provisioning`, three new tables:** `bulk_jobs`,
`bulk_job_items`, `activation_requests`. Additive; `orders`, `order_items` and
`ledger_reservations` are referenced and otherwise untouched. Documented as
[data-model §6.55](../../data-model.md) and [api-spec §7.38](../../api-spec.md).

**Refactor within scope:** `CatalogService.sale_terms` — a public lookup for
"who sells this here, and for how much", so a bulk order and a consumer quote
cannot drift into two answers about the same product on the same day.

**The decisions a reviewer should look at:**

1. **A bulk order is fifty independent purchases.** The items carry the state;
   the job carries counts written in the same transaction as the item they
   describe. "Forty-seven provisioned, two unknown, one invalid" is expressible,
   and a job that rounded that to "failed" would misreport forty-seven working
   lines.
2. **One hold per line, and a retry takes a *fresh* one.** This is the defect
   the fifty-recipient scenario caught during development: `mark_failed`
   releases the hold, so a retry that reused `reservation_id` would provision a
   line the organization is not holding money for. `_ensure_hold` is the single
   place that decides a line is funded, and an outstanding balance is the only
   thing that counts.
3. **`unknown` is not reachable from the retry path.** `resumable` returns two
   lists and an unknown item is only ever in the first. A caller that ignored
   reconciliation entirely still could not buy a second profile for that
   recipient — not unlikely, *not possible*.
4. **Silence is not permission.** A reconciler that returns `None` leaves the
   item unknown with its hold intact, rather than assuming either outcome.
5. **`provision` is idempotent.** A worker that lost the response and retried is
   asking a question, not making a second purchase, so a finished job answers
   with its progress instead of a 409.
6. **A provisioned line is refunded, not cancelled.** Releasing a hold for a
   service the customer has is giving money away; unwinding it is chunk 14's
   policy path, with a human and a reason. An `unknown` line is refused too — the
   supplier may have provisioned it.
7. **Recipients are not users.** `order_items.recipient_user_id` stays null
   until an activation request is redeemed. Inventing an account at purchase
   would bind a line to an identity nobody authenticated.
8. **Redeeming is not an administrator action.** The recipient endpoint takes an
   ordinary session and no organization permission. Requiring tenant membership
   to accept a work SIM would be exactly backwards.
9. **A request is not an installation.** `activation_requests` records the
   invitation and its redemption only. Installation is chunk 15's
   `esim_installations`; attachment is `carrier_lines`; the view carries
   neither.
10. **Internet-only assignment buys no carrier profile.** A `VOICE` product takes
    the entitlement path and stops, per the calling amendment.

## Acceptance evidence

| Criterion | Test or manual procedure | Result | Evidence path |
|---|---|---|---|
| 50-recipient fixture scenario with invalid recipients, partial supplier failure, lost responses, duplicate submissions and worker restart | `TestFiftyRecipients::test_the_whole_scenario` — 46 serviceable, 2 who left, 1 unreachable, 1 from another organization; one line failed and one lost after provisioning; a **new service instance** resumes, remembering nothing | PASS | `apps/api/tests/test_bulk_provisioning_postgres.py` |
| No duplicate charge | 46 live holds for 46 lines; the replayed submission returns the same job; `provision` twice creates no second order item | PASS | same, plus `test_provisioning_twice_does_not_create_a_second_line` |
| No duplicate profile/assignment | `ux_bulk_job_items_order_item` (one order item per line) and `uq_bulk_job_items_person` (one line per person per job), both asserted | PASS | same |
| No cross-tenant recipient reuse | `test_another_organizations_person_cannot_be_a_recipient` (invalid, indistinguishable from missing) and `test_another_organizations_job_is_not_found` | PASS | both test files |
| Correct partial balances | `test_partial_funding_serves_who_it_can`, `test_cancelling_one_line_releases_only_that_hold`, and the scenario's closing balance assertions | PASS | `test_bulk_provisioning_postgres.py` |
| Correct progress | `test_a_partially_completed_job_says_so` — a job with two of three lines is `partially_completed`, not `completed` and not failed | PASS | same |
| Resume only failed/unknown, reconciling before retrying | `TestLostAndFailedOutcomes` (6 tests) | PASS | same |
| Partial cancellation without reversing consumed services | `test_a_provisioned_line_is_refunded_not_cancelled`, `test_a_reserved_line_can_be_withdrawn` | PASS | both files |
| Recipient-bound invitation and redemption | `TestActivationRequests` (7 tests) + `TestActivationHandoff` (6 HTTP tests) | PASS | both files |
| Activation request not represented as installation | `test_a_request_is_not_an_installation`, `test_an_activation_request_is_not_an_installation` — asserts `esim_installations` and `carrier_lines` are **empty** after a redeemed request | PASS | both files |
| Internet-only assignment without a carrier profile | `test_an_internet_only_product_needs_no_carrier_profile` | PASS | `test_bulk_provisioning_postgres.py` |
| Authorized assignment only | `TestOnlyBuyersMayBuy` — billing and member sessions refused | PASS | `apps/api/tests/test_bulk_api.py` |
| Employee installation handoff | The activation request **is** the handoff; the installation step itself is chunk 15/20's surface and unchanged | PARTIAL | see *Known gaps* |

**Everything above is local.** No supplier was called, because none is wired —
see below. The scenario's "partial supplier failure" and "lost response" are
driven through `mark_failed` and `mark_unknown`, which are the same entry points
a real supplier path would use.

## Checks actually run

| Command and working directory | Result | Relevant output |
|---|---|---|
| `ruff check .` — `apps/api` | PASS | `All checks passed!` |
| `mypy app` — `apps/api` | PASS | `Success: no issues found in 164 source files` |
| `pytest tests/test_bulk_provisioning_postgres.py -q` | PASS | 28 passed |
| `pytest tests/test_bulk_api.py -q` | PASS | 20 passed |
| `pytest tests/test_i18n_api.py -q` | PASS | catalog parity holds across `en`/`fr` |
| `pytest --cov=app` — `apps/api` | PASS | 1240 passed, 5 skipped, 0 failed; total coverage **90.07%** against the 85% gate |
| `alembic upgrade head` — `apps/api` | PASS | `0039_bulk_provisioning`; three tables present |
| `PYTHONPATH=. python scripts/export_api_spec.py` | PASS | `docs/api-spec.yaml` **+711 / −0**, insertions only |

Environment: PostgreSQL 16 in the local `damdam-test-pg` container on port
55434, database `damdam_c23`. Interpreter `~/damdam-api-venv/bin/python`.

**Not run, and why:**

- **Any supplier call.** No `ConnectivityAdapter` is wired into this chunk's
  application state — see *Known gaps* — so nothing was purchased from a
  carrier, and no live provisioning evidence is claimed.
- **The dashboard.** No dashboard surface was built for bulk orders in this
  chunk. The API is complete and the screens are a separate piece of work;
  building them without a supplier would produce a page whose main button does
  half of what it says.
- **Throughput at fifty recipients.** The scenario proves correctness, not
  speed. Provisioning writes and flushes per item deliberately, which is right
  and not fast.

## Migrations and compatibility

- **Numbering.** `0039` is also taken by `0039_call_settlement` (V03) on an
  unmerged branch, and this branch's `0038` is chunk 22's. Whichever stack
  merges last renumbers.
- **Fresh install:** three tables, three enums. Every endpoint answers.
- **Old-data upgrade:** additive only; nothing is backfilled and no existing row
  is read differently. Orders placed before this chunk are untouched and are not
  retrofitted into bulk jobs.
- **Old-client compatibility:** every endpoint is new. **One near-miss worth
  recording:** the new redeem request schema was initially named
  `ActivationRedeemRequest`, which chunk 15 already publishes — two Pydantic
  models sharing a class name silently collapse into one entry in the generated
  OpenAPI, and the drift check caught it as ten deleted lines. It is now
  `BulkActivationRedeemRequest` and the spec diff is insertions-only.
- **Rollback:** `alembic downgrade 0038_organization_people` drops the three
  tables. The orders and order items a bulk job produced **stay** — they are
  ordinary order rows, and dropping them because the bulk surface was removed is
  what `AGENTS.md` forbids.
- **Configuration:** none added. No new secret names.
- **Dependencies:** none added.

## UI or integration evidence

- **None.** This chunk is API and schema only; the dashboard screens are open
  work. Every behaviour claimed above is proved through the service or the HTTP
  surface.
- **Sensitive fields:** the activation token is returned once and stored only as
  a SHA-256 hash. It appears in no log, no fixture and no document — including
  this one.

## Known gaps and handoff

- **No supplier is wired, and provisioning stops short for carrier products.**
  `ConnectivityService` needs a `FulfilmentService` and an adapter, and that
  application wiring belongs to chunk 20, which is not in this stack. So
  `main.py` constructs the bulk service with `connectivity=None`: a carrier line
  is funded and ordered and then **stays `ordered`**, which is the honest state
  rather than a pretended one. The service tests supply a real
  `ConnectivityService`, so the grant path is exercised. **This is the first
  thing to connect once chunk 20 lands, and until then a carrier bulk order is
  preparatory only.**
- **Nothing sends the invitation.** `issue_activation_request` returns the token
  to the caller; no email or SMS is dispatched. The notification path exists
  (chunk 21's preferences, the notification service) and wiring it means
  deciding what a work-line invitation says and in whose name — a product
  question, not a plumbing one.
- **`resume` has no reconciler in the HTTP path.** The route calls `resume`
  without one, so an unknown item stays unknown and visible. That is correct and
  incomplete: reconciliation talks to a supplier, and there is no supplier.
- **Partial refund is refused, not performed.** Cancelling a provisioned line
  returns 409 and points at policy. Actually issuing the refund is chunk 14's
  surface and chunk 24's scope; wiring it here would have this chunk deciding a
  refund policy it was not asked to decide.
- **A job's counters are maintained by the service, not by the database.** They
  are written in the same transaction as their item, so they cannot drift under
  normal operation — but a direct `UPDATE` on `bulk_job_items` would desynchronise
  them. A trigger or a view is the stricter answer; flagged rather than built.
- **For Codex:** the place to push hardest is `resume`'s third loop, which
  picks up items left `reserved` by a crash between funding and provisioning.
  It re-enters `_provision_item`, which is idempotent through `_order_item_for`
  — but that idempotency is by lookup, and two workers resuming the same job
  concurrently is not something this chunk tested. A job-level advisory lock is
  the obvious fix and I did not add one speculatively.
- **No merge, deployment, store submission or production deletion was
  performed**, and none is authorized by this assignment.

Claude's handoff is a request for review, not acceptance.
