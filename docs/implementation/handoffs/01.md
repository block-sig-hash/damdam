# Claude implementation handoff — chunk 01

## Identity

- **Chunk and registered story:** Chunk 01 — Reset product specifications and
  working rules. Story **US-27** (`docs/prd.md` §10.5). Parent PR-01.
- **Branch/worktree path:** `chunk/01-scope-and-specifications` in the local
  clone `/tmp/damdam-review-20260907`. No PR exists; nothing was pushed.
- **Base SHA:** `6790c74707a0f3e52cfedb36bc173ec83ca26663`
  (`origin/develop`, confirmed current after `git fetch --all --prune --tags`
  on 2026-09-08).
- **Head SHA:** `0f3331a907783edc75663ded8e6cc85208961ec9` — the complete chunk
  content. This handoff file is added by a following `docs:` commit on the same
  branch; that later commit adds **only** `handoffs/01.md`. Review the branch
  tip; the reviewable content diff is `6790c74..0f3331a`.
- **Working tree:** clean at `0f3331a`. No stashes, no other worktrees, no
  uncommitted changes. Untracked build artifacts were not created.
- **Accepted dependency reviews:** none required — chunk 01 has no dependencies.
- **Status:** **READY_FOR_REVIEW**

## Result

**Previous behavior.** The repository described a Hajj-pilgrim connectivity
product with family contacts, offline SOS, check-ins, verified Nigerian caller
ID and WebRTC app calling, sold through tour operators (HTOs). `AGENTS.md`
assigned mobile screens to Codex against a Claude-authored design system, with
Claude reviewing, and told agents to default to Sonnet and let Codex escalate.
`pre-pilot-checklist.md` imposed a pilot feature freeze. All external decisions
were implicit.

**Resulting behavior.** The repository now describes a global consumer plus
enterprise/government eSIM and carrier-voice product, states one working rule
(Claude implements, Codex independently reviews and may refactor), registers
`US-27`–`US-43` with acceptance criteria, classifies every legacy reference, and
records D1–D6 as open with owners and required evidence. The build sequence, the
current CI baseline and the five original findings' traceability all live in
`docs/implementation/`.

**No spec section was deleted.** Legacy content is retained and classified,
because deleting decision history would destroy the traceability the repository's
amendment convention exists to preserve. Later chunks retire behavior and then
amend the specs that describe it.

### Affected files

Modified (all documentation):

| File | Change |
|---|---|
| `AGENTS.md` | Rewritten. One implement/review rule; revised strict-TDD categories; retained abstraction, money, security, migration and secret-handling rules; product boundaries; per-chunk workflow |
| `README.md` | Product description, agent split, repo structure, status |
| `docs/README.md` | Rewritten index: current-build section, per-spec reset status, reference/historical section. Adds the four documents the old index omitted |
| `docs/prd.md` | New §10 — reset definition, user types, journeys, US-01–26 disposition, US-27–43 with acceptance criteria |
| `docs/data-model.md` | New §6.42 amendment |
| `docs/api-spec.md` | New §7.26 amendment (`api-spec.yaml` untouched, so the CI drift check is unaffected) |
| `docs/frontend-mobile.md` | New §8.9 amendment |
| `docs/frontend-dashboard.md` | New §9.9 amendment |
| `docs/security.md` | New §10.14 amendment — no control relaxed |
| `docs/infrastructure.md` | New §11.15 amendment carrying the observed baseline |
| `docs/scaling-infrastructure.md` | New §12.11 amendment |
| `docs/corporate-structure.md` | New §13.5 amendment — UAE structure reclassified as a recommendation, not a decision |
| `docs/testing-qa.md` | New §14.13 amendment — revised strict-TDD table, PostgreSQL rule, current failures |
| `docs/design-system.md` | New §15.3 amendment |
| `docs/localization.md` | New §9 amendment |
| `docs/i18n-scoping.md` | New §11 amendment |
| `docs/verified-cli-scoping.md` | New §7 amendment — deferred in full |
| `docs/pre-pilot-checklist.md` | Closing amendment — superseded as a gate, feature freeze lifted |

Added:

| File | Purpose |
|---|---|
| `docs/implementation/` (pack) | `README.md`, `IMPLEMENTATION-PLAN.md`, `STATUS.md`, `REVIEW.md`, `HANDOFF-TEMPLATE.md`, `chunks/01`–`30`, `damdam-product-shape.md`, `damdam-payment-provider-enquiry.md` — copied verbatim except `STATUS.md` |
| `docs/implementation/BASELINE.md` | Refreshed baseline: base SHA, deployment evidence, current vs. historical CI |
| `docs/implementation/DECISIONS.md` | D1–D6 register, all OPEN, with proposed defaults labelled as proposals |
| `docs/implementation/STORY-MAP.md` | Chunk → story map, ID-collision check, five-findings traceability |
| `docs/implementation/SCOPE-DISPOSITION.md` | Legacy concept classification and per-spec inventory |
| `docs/implementation/MODULE-OWNERSHIP.md` | Module boundaries, existing-code mapping, journey routing |
| `docs/implementation/tools/check-docs-links.sh` | Relative-link validator (not wired into CI — chunk 02's scope) |
| `docs/implementation/handoffs/README.md`, `reviews/README.md` | Directory conventions |

**Public contracts:** none changed. No endpoint, schema, migration, workflow or
script was modified.

### Deliberately deferred

- **`scripts/validate-release-signoff.sh` and `docs/release-signoffs/TEMPLATE.md`
  were not changed.** They still require "Offline check-in survival" and
  "Offline SOS survival" rows, for features this reset retires, and `main`'s
  branch protection genuinely blocks a `staging → main` merge without them.
  Release automation is **chunk 27**'s scope, and weakening a release gate from
  a documentation chunk should be reviewed on its own. Recorded in
  `STATUS.md`, `pre-pilot-checklist.md` and `infrastructure.md` §11.15; closed
  by AC-42.5.
- **The link checker is not wired into CI.** CI configuration is chunk 02's
  scope.
- **`.github/workflows/claude-review.yml` was not changed.** Its role changes now
  that Claude implements rather than reviews; flagged in `AGENTS.md` for
  chunk 02.
- **No spec was rewritten in place.** Retirement and rewrite belong to chunks
  04–24.

### Deviations from the assignment

One. The assignment says "verify [the likely starting paths] in the actual
current checkout" and lists `docs/README.md` and the spec suite; it does not
name `docs/implementation/tools/`. Adding a link validator there is an addition
beyond the literal list, made so that AC-27.7 is reproducible by the reviewer
rather than being a claim in this handoff. It is not wired into any pipeline and
changes no behavior.

## Acceptance evidence

| Criterion | Test or manual procedure | Result | Evidence |
|---|---|---|---|
| AC-27.1 — one implement/review rule, no conflicting ownership table | Read `AGENTS.md`; `grep -n 'Primary implementer\|Codex, against\|design-system.md first' AGENTS.md` returns nothing | **PASS** | `AGENTS.md` "Who implements what"; `MODULE-OWNERSHIP.md` separates agent ownership from module ownership |
| AC-27.2 — baseline refreshed and recorded, nothing described as fixed | `git fetch --all --prune --tags`; `gh run list`; `gh run view 34198388517`; `gh api .../deployments` | **PASS** | `BASELINE.md`; both original CI findings recorded open |
| AC-27.3 — pack present in `docs/implementation/`, including the plan | `diff -r /home/iadamu/artifacts/damdam-build-pack docs/implementation` | **PASS** — only `STATUS.md` differs, intentionally | Tree listed above |
| AC-27.4a — every chunk maps to a registered story | `STORY-MAP.md` chunk table, 30 rows | **PASS** | `STORY-MAP.md` |
| AC-27.4b — story-ID collision check | `grep -ohE '\bUS-[0-9]+' docs/*.md create-issues.sh \| sort -uV` → `US-01`…`US-26`, no gaps | **PASS** — `US-27`+ free, adopted unchanged; no ID reused | `STORY-MAP.md` |
| AC-27.4c — legacy references classified | Per-spec inventory across 15 specs for Hajj/family/SOS/check-in/WebRTC/verified-CLI/HTO; each classified retired / generalized / deferred / historical | **PASS** | `SCOPE-DISPOSITION.md` |
| AC-27.5 — D1–D6 with owners, evidence, affected chunks; proposed defaults labelled | Read `DECISIONS.md` | **PASS** — all six OPEN; five proposed defaults labelled as proposals, no founder approval claimed | `DECISIONS.md` |
| AC-27.6a — module ownership defined | Read `MODULE-OWNERSHIP.md` | **PASS** | 10 target modules, existing-code mapping, cross-cutting rules |
| AC-27.6b — revised journeys defined | Read `prd.md` §10.3 and `MODULE-OWNERSHIP.md` journey table | **PASS** | Four journeys with chunk and story routing |
| AC-27.6c — docs index and release criteria coherent | Read `docs/README.md`, `pre-pilot-checklist.md` amendment | **PASS with one known gap** — the signoff validator still demands retired evidence; deferred to chunk 27 and recorded in three places | `docs/README.md`; `STATUS.md` |
| AC-27.7 — documentation links resolve | `bash docs/implementation/tools/check-docs-links.sh` | **PASS** — 174 relative links checked, 0 broken | Command output below |
| Five findings traced to a chunk and an acceptance test | Read `STORY-MAP.md` | **PASS** | Each finding names owning chunk, story, criterion and its acceptance test; none declared fixed |

Everything above is documentation inspection and static validation. Nothing here
is simulated vendor behavior, sandbox, staging or physical evidence, because this
chunk produces none. The CI facts in `BASELINE.md` come from GitHub Actions runs
executed by the repository's own pipeline, not from a local run.

## Checks actually run

| Command and working directory | Result | Output / artifact |
|---|---|---|
| `git fetch --all --prune --tags` (repo root) | exit 0, no new refs | `origin/develop` = `6790c74`, unchanged since the historical review |
| `git rev-parse origin/develop` | exit 0 | `6790c74707a0f3e52cfedb36bc173ec83ca26663` |
| `bash docs/implementation/tools/check-docs-links.sh` | exit 0 | `checked 174 relative links` / `OK: no broken relative links` |
| `diff -r /home/iadamu/artifacts/damdam-build-pack docs/implementation` | differs only in `STATUS.md` | Intentional: chunk 01 status recorded |
| `gh run list --limit 8` | exit 0 | Every nightly run 2026-09-01…2026-09-08 concluded **failure** |
| `gh run view 34198388517 --json headSha,conclusion,jobs` | exit 0 | Head `6790c74`; API job failure; iOS screenshot job **success** |
| `gh run view 34198388517 --log-failed` | exit 0 | `1 failed, 335 passed`; coverage 87.13% vs. 85% threshold |
| `gh api repos/block-sig-hash/damdam/deployments` | exit 0 | Latest staging deployment 2026-07-28; no production deployment in history |

### Checks NOT run, and why

- **`pytest --cov=app`, `ruff`, `mypy` (apps/api); `npm test/lint/type-check`
  (apps/mobile, apps/dashboard); `npm run build` (dashboard); OpenAPI drift.**
  **NOT RUN locally.** Chunk 01 changes no application code, no
  `api-spec.yaml` and no CI configuration. `.github/workflows/ci.yml`'s
  `dorny/paths-filter` gates all four jobs on `apps/**`, `docker/**`,
  compose files and the workflow file itself, so a documentation-only change
  **skips every one of them** — there is no repository-required check that this
  chunk triggers. The current state of those suites is recorded from CI in
  `BASELINE.md` rather than claimed from a local run.
- **Independent local reproduction of the failing API test.** Attempted; see
  "Known gaps" below.
- **Screenshot, native build, device and vendor checks.** Not applicable and not
  authorized for this chunk.

No check was disabled, no threshold lowered and no coverage requirement reduced.

## Migrations and compatibility

- **Fresh install and old-data upgrade:** not applicable — no schema change, no
  migration, no backfill.
- **Historical data:** untouched. The amendments explicitly require later chunks
  to preserve order, payment and audit history through retirement, and to
  preserve historical NGN receipts, balances and identifiers through the
  multi-currency migration.
- **Old-client/worker compatibility:** unaffected. `api-spec.yaml` is unchanged,
  so no contract moved.
- **Rollback:** `git revert 0f3331a`. No irreversible step was taken. Tag
  `v0.1.0-rc.1` was not moved, rewritten or deleted; no branch was reset,
  rebased or force-updated; nothing was pushed.
- **Configuration and secrets:** none added, referenced or changed. No secret
  name appears in this chunk.
- **Dependencies:** none added. `docs/implementation/tools/check-docs-links.sh`
  uses only bash, `find`, `grep` and `sed`.

## UI or integration evidence

- **Screenshots / device / locale:** none — no UI changed.
- **Contract documentation source and date:** the Telnyx documentation review
  carried in `IMPLEMENTATION-PLAN.md` §5 is dated **8 September 2026** and is a
  reading of public documentation. It is recorded in `DECISIONS.md` as **not**
  moving D1, and the plan's own caveats (VoLTE labelled beta; the
  650+/180+ figure is a data claim, not evidence for voice, inbound calls, local
  numbers or permanent roaming) are carried forward verbatim rather than
  softened.
- **Fixture versus actual provider response:** not applicable.
- **Physical device / network / payment evidence:** none. None was authorized
  and none is claimed.
- **Sensitive fields:** no secret, credential, activation code, customer record
  or payment payload appears in any added file. The GitHub Actions run IDs and
  artifact IDs recorded in `BASELINE.md` are public repository metadata.

## Known gaps and handoff

### Open defects and their impact

Both original CI findings remain **open**, and this chunk fixes neither:

1. `tests/test_auth_api.py::test_otp_request_and_verify_contract` fails on
   `develop` @ `6790c74` — `ExpiredSignatureError` → `InvalidRefreshTokenError`
   → `OTPError`. Reproduced by CI on 2026-09-07 and again on 2026-09-08.
   Chunk 02, AC-42.1.
2. The iOS screenshot job is **intermittent**, not deterministically broken. It
   failed on 2026-09-07 and succeeded on 2026-09-08 on the identical commit,
   uploading 33 PNGs. This is a **material correction** to the historical
   "0/32" record: chunk 02 must add a nonempty-manifest assertion so
   intermittency surfaces as a failure, not only "make screenshots generate".
   Chunk 02, AC-42.2.

Findings 1–4 are unverified by this chunk. They are historical observations from
the 7 September review, traced in `STORY-MAP.md` to chunks 04, 18–20, 16 and
11/15 with named acceptance tests. None is declared fixed.

### External gates and decisions

All of **D1–D6 are OPEN** (`DECISIONS.md`), each with owner, required evidence
and affected chunks. Chunk 01 itself is not gated by any of them.

Five **proposed defaults** are recorded as proposals awaiting the founder, not
approvals: full check-in/welfare removal, email-based recovery, a new assigned
number with no +234 retention promise, prepaid charging, and (already a user
decision, not a proposal) family/SOS removal. Chunk 04 must not begin welfare
removal until the check-in proposal is confirmed.

### Remaining test and evidence work

- Independent local reproduction of the API auth failure was **attempted and
  abandoned**: `pip install -r requirements.txt -r requirements-dev.txt` into a
  fresh virtualenv exceeded the session's time budget and was killed, so no
  local test was ever executed. The scratch virtualenv and the PostgreSQL 16
  container started for the attempt were both removed; **no environment side
  effect remains**. The CI evidence above stands on its own and comes from the
  repository's own pipeline. Chunk 02 owns local reproduction under a controlled
  test clock.
- No documentation-link check runs in CI yet. Chunk 02 should wire
  `docs/implementation/tools/check-docs-links.sh` in, or replace it.

### Refactoring concerns and tradeoffs for Codex

1. **Amendment-versus-rewrite.** I appended classified amendments rather than
   rewriting the specs. This keeps 15 documents internally contradictory until
   chunks 04–24 retire the behavior they describe, which is a real cost:
   `SCOPE-DISPOSITION.md` and each spec's amendment are load-bearing for anyone
   reading an older section. If you think a spec should be rewritten now instead,
   say so — but rewriting before the behavior is retired would make the specs
   describe code that does not exist.
2. **`prd.md` §10 is long.** Registering 17 stories with criteria in one
   amendment section is bulky. Splitting stories into their own file would break
   `AGENTS.md`'s existing "find the `US-XX` in `prd.md`" convention, so I kept
   them together. Worth a second opinion.
3. **The chunk-to-story map is many-to-one.** 30 chunks map onto 17 stories
   because the chunks are smaller review units cut from the plan's PR-sized
   stories. Chunk 08 is mapped to `US-27` rather than getting its own story;
   check that reads correctly to you.
4. **The signoff validator gap is the one deliberate incoherence** in this
   chunk. It is recorded in three places rather than fixed. If you judge it
   should be fixed here, that is a defensible finding — I judged it chunk 27's
   scope, and it is currently a live merge blocker for a retired feature.
5. **The link checker is a bash script inside a docs directory.** If you would
   rather it not exist until chunk 02 wires it in, removing it costs only
   AC-27.7's reproducibility.

### Authorization

No merge, deployment, push, store submission, outgoing vendor message, live
purchase or production data deletion was performed. The branch exists only in
the local clone. Read-only GitHub API queries (`gh run list`, `gh run view`,
`gh api .../deployments`) were used to gather CI and deployment evidence.

**Environment caveat:** the working checkout is `/tmp/damdam-review-20260907`,
and `/tmp` is not durable across a host restart. Relocate or push the branch
before relying on it.

This handoff is a request for review, not acceptance. Chunk 02 has not been
started.
